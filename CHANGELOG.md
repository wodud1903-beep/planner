# 변경 이력

버전마다 무엇이 달라졌는지 적어 둔 곳입니다. PC 앱의 [변경 이력] 과 모바일 웹앱이
이 파일 하나를 읽어 보여 줍니다.

## v1.16.2 — 기본 테마를 한 단계 밝게 (2026-09-17)

■ 기본 (밝은 청회색) 이 밝아졌다 (PC)
  화면 전체에 옅은 회색 필터를 씌운 것처럼 보이던 톤을 한 단계 올렸다.
  · 패널 #F3F6FA → #FBFCFE (거의 흰색)
  · 입력칸 #F7F9FC → #FFFFFF (순백)
  · 전체 배경·표머리·단추·탭·브리핑 배경도 같이 한 단계씩
  달력 칸 색(파랑·초록·주황·보라)과 오늘·내일 색은 **그대로** 뒀다.

■ 밝게 하면서 지킨 것
  밝게만 올리면 패널·바탕·테두리가 서로 붙어 표와 달력의 칸 선이 사라진다.
  그래서 테두리·격자는 같이 올리지 않고 패널 대비 1.45 / 1.25 를 남겼다.
  글자 대비는 오히려 올라갔다 — 본문 10.13 → 10.70, 흐린 글자 6.45 → 6.81.
  검사(verify_dark [8])에 이 선들을 못 박아 뒀다.

## v1.16.1 — 캘린더를 따로 뜨는 창으로 (2026-09-17)

■ 캘린더가 **제 창**으로 뜬다 (PC)
  지금까지 캘린더는 메인 창 안에 얹힌 자식 위젯이었다. 그래서 메인 창보다
  넓게 만들 수 없었고, 오른쪽 일정 열(340px)이 딱 그만큼 잘려 나갔다 —
  손으로 메인 창을 넓혀야 일정이 보였다.
  이제 따로 뜨는 창이라 제 크기(1440x920, 화면이 좁으면 화면에 맞춤)로 화면
  가운데에 열린다. 메인 창 크기를 바꿔도 캘린더는 그대로다.

■ 오른쪽 일정 목록이 읽기 좋아졌다 (PC)
  · 글씨를 크게(12pt 굵게) 하고 줄 높이도 그만큼 띄웠다.
  · 줄마다 **라벨색**을 칠했다. 색은 달력 칸의 띠와 같은 것을 같은 차례로 쓴다 —
    달력의 셋째 띠가 목록의 셋째 줄과 같은 색이다.
  · 날짜 제목도 같이 키웠다.

## v1.16.0 — 폰에서 고객 정보 고치기 (2026-09-17)

■ ⚠️ 폰에서 한 번 다시 로그인해야 합니다
  고치려면 시트에 **쓰는** 권한이 필요한데, 지금 폰에 있는 로그인에는 그 권한이
  없습니다. 앱을 열면 위에 [다시 로그인] 이 뜹니다. 한 번 눌러 주세요.

■ 폰에서 PC 와 똑같이 고친다
  고객 상세에 [고치기] 가 생겼다. 고객명·금융사·차종·차량가격·금융수수료·
  대리점 수당·특판/대리점·계약일·출고일·진행현황·계약조건·내용·출고유형.
  · 금융사·특판/대리점·진행현황·출고유형은 **시트에 이미 있는 값**을 후보로
    보여 준다(직접 쳐 넣어도 된다). 자주 쓰는 것이 위로 온다.
  · 금액은 치는 동안 1,000 단위가 붙는다.
  · 날짜는 달력에서 고르고, [미정] 을 켜면 비운다.
  · 저장하면 화면을 먼저 바꾸고 뒤에서 보낸다. 실패하면 되돌리고 그렇게 말한다.

■ 시트를 망가뜨리지 않으려고 지킨 것
  이 판은 이 앱이 **처음으로 시트에 쓰는** 판이다. 잘못 쓰면 수식이 값으로 덮이고
  되돌리기 어렵다. 그래서 규칙을 새로 만들지 않고 PC 가 쓰던 것을 그대로 옮겼다.
  · 직접기재 칸만 **칸 단위로** 쓴다. 줄을 통째로 쓰지 않는다.
    순번·합계·고객센터·사고접수·안내멘트·등록완료는 시트가 계산하는 칸이라
    **건드리지 않는다.**
  · **고친 칸만** 보낸다. 안 건드린 칸은 아예 요청에 넣지 않는다.
  · 금액은 콤마를 떼고 숫자로 보낸다(안 그러면 통화 서식이 깨진다).
  · 날짜는 시트 서식 그대로 "2026. 9. 5" — 0 을 채우지 않는다.
  · **견적서는 폰에서 못 고친다.** 셀 안 그림이라 잘못 건드리면 지워진다.
    보기만 되고, 바꾸실 일이 있으면 PC 에서 해 주세요.

■ 그 사이 누가 고쳤으면 묻는다 (PC 에 없는 장치)
  폰은 저장해 둔 목록으로 화면을 그린다. 한 시간 전 것을 보고 있는데 그 사이
  PC 에서 같은 고객을 고쳤다면, 그냥 쓰면 그 수정이 조용히 지워진다.
  그래서 저장 직전에 그 줄만 다시 읽어, **내가 고치려는 칸이 그 사이 바뀌었으면**
  덮어쓸지 묻는다. 안 바뀌었으면 아무것도 묻지 않는다.

■ 검사
  가짜 시트를 물려 **실제로 보내는 요청을 뜯어본다** — 수식 칸이 하나도 안
  들어갔는지, 견적서 칸이 안 들어갔는지, 금액에 콤마가 없는지, 날짜 서식이 맞는지,
  안 고친 칸을 안 보내는지, 그 사이 바뀌었을 때 묻고 취소하면 안 쓰는지. 18가지.
  옮긴 계산(날짜 서식·후보 목록·열 글자)은 파이썬 정답표와 대조한다 —
  후보 차례가 PC 와 달라지던 것을 이 대조가 잡았다.

## v1.15.0 — 폰에서 주간 요약 (2026-09-16)

■ 주간 요약을 폰에서 본다
  PC 앱에만 있던 주간 요약이 폰에도 생겼다. [고객] 탭 아래 **[주간 요약]** 을 누른다.
  · 이번 주 출고 (수수료 합계까지)
  · 이번 주 계약
  · 진행 중 (출고 대기) — 오래 묵은 건부터, 며칠째인지 함께
  · 6개월 이내 만기 예정 — 급한 순으로, D-며칠
  고객을 누르면 그 고객 상세로 바로 넘어간다.

■ 새로 받아 오는 것이 없다
  고객 탭이 이미 읽어 둔 줄로 계산만 한다. 그래서 **오프라인에서도 열리고**
  누를 때 기다림이 없다.

■ PC 와 한 건도 다르지 않게
  주 경계(월요일 시작), 취소 건 제외, 보류 건 제외, 만기 계산(출고일 + 계약기간,
  말일 보정) 같은 규칙이 어긋나면 건수가 조용히 달라진다. 파이썬이 낸 정답을
  자바스크립트가 맞히도록 다섯 날짜치를 통째로 대조한다.
  · 옮긴 계산은 파이썬과 **글자까지 같게** 뒀다. 폰에 안 맞는 안내말(PC 의 탭
    이름을 가리키는 문구)만 화면 쪽에서 바꿔 넣는다. 처음엔 옮긴 쪽에서 고쳤다가
    대조가 통째로 어긋났다 — 그러면 진짜 차이를 못 보게 된다.

## v1.14.0 — 출고일이 캘린더로, 캘린더에서 고객으로 (2026-09-16)

■ 출고일을 정하면 캘린더에 저절로 올라간다
  고객 출고일을 '미정' 이 아닌 날짜로 바꾸면 그 날짜에 종일 일정이 생긴다.
  제목은 "<고객> 출고" 다.
  · 이 모양으로 맞춘 데는 이유가 있다. 앱은 이미 "…출고" 로 끝나는 일정을 찾아
    [출고 1개월] 팔로업 할일을 만들고 있었다. 같은 모양으로 만드니
    **팔로업이 저절로 따라붙는다.** 새로 만든 배관이 없다.
  · 출고일을 다른 날로 바꾸면 그 일정을 **옮긴다**. 지웠다 새로 만들면
    팔로업 할일이 두 번 생긴다.
  · '미정' 으로 되돌리면 지운다. 이름만 바꾸면 제목만 고친다.
  · **이미 출고일이 적혀 있던 고객은 건드리지 않는다.** 앞으로 바뀌는 것만
    올라간다. 수백 명에게 한꺼번에 일정이 생기면 되돌리기 어렵다.
  · 내가 만든 일정만 손댄다. 손으로 만들어 두신 일정은 그대로 둔다.
  · [설정] 에서 끌 수 있다.
  · 영문 상호처럼 이름이 안 읽히는 고객은 만들지 않는다 — 만들어 봐야
    팔로업이 안 걸리고 캘린더에 뜻 모를 줄만 남는다.

■ 캘린더 창 — 그 날 일정이 오른쪽 한 열로
  예전엔 달력 아래 좁은 칸(120px)에 눕혀 두어, 세 건만 넘어도 스크롤해야 했고
  긴 상호는 옆으로 잘렸다. 이제 오른쪽에 세로로 길게 선다(같은 화면에서 480px).
  · 가운데 경계선을 끌어 폭을 정할 수 있고, 그 폭을 기억한다.
  · 창이 그만큼 넓게 열린다.
  · 더블클릭은 예전과 똑같이 수정 창을 연다.

■ 일정에서 그 고객으로 바로 간다
  오른쪽 목록에서 **우클릭** 하면 [일정 수정] 과 [해당 일정으로 이동] 이 나온다.
  '이동' 을 누르면 고객관리로 옮겨 가 그 고객의 출고정보를 연다.
  · "홍길동 출고" 도, "[출고 1개월] 홍길동" 도 둘 다 찾아간다.
  · 고객 칸이 "박성일 / 박가네 백년약초" 처럼 되어 있어도 찾는다.
  · 같은 이름이 둘이면 **묻는다.** 차종과 출고일을 같이 보여 주고 고르게 한다 —
    말없이 첫 번째를 열면 남의 고객을 고치게 될 수 있다.
  · 고객과 이어지지 않은 일정에서는 메뉴가 눌리지 않는다.

■ 곁다리로 고친 것
  캘린더 날짜 머리글의 요일이 영어로 나오고 있었다("2026-09-20 (Sun) 일정").
  한국어로 바꾸고 그 날 건수도 같이 적는다 — "2026-09-20 (일) 일정  4건".

■ 검사를 프로그램과 같은 곳에 뒀다
  PC 앱 검사 12벌이 작업용 임시 폴더에만 있었다. 웹 검사는 저장소에 있는데
  파이썬 검사만 밖에 있어서, 작업 환경이 다시 뜨면 통째로 사라질 자리였다.
  tools/verify/ 로 옮기고 한 번에 돌리는 도구를 뒀다. 이번에 새로 쓴 것까지
  15벌이 30초에 돈다.

## v1.13.0 — 폰에서도 쓴다 — 모바일 웹앱 (2026-09-15)

■ 왜
  PC 앱의 기능을 갤럭시 폴드7 에서도 쓰려고 만들었다. 안드로이드 앱(APK) 대신
  **웹앱(PWA)** 으로 했다 — 스토어 심사도 설치 승인도 없이, 고치면 다음에 열 때
  바로 최신이 된다. 주소 하나로 연다.

      https://wodud1903-beep.github.io/planner/

  홈화면에 추가하면 앱처럼 뜬다. 자바스크립트 전부 합쳐 70kB 남짓이고
  웹폰트를 안 쓴다(한글 서브셋이 1~3MB 라 앱보다 커진다).

■ 폰에서 되는 것 — 탭 여섯 개
  · 일정 / 할일
      앞으로 30일 일정을 날짜별로 묶어 보여준다. 오늘·내일은 말로 적고 색을 달리한다.
      할일은 **동그라미를 눌러 그 자리에서 '했다' 로 바꾼다.** 밖에서 엄지로 누르는 게
      이 화면의 쓸모라 단추를 크게 뒀다.
      캘린더를 하나라도 못 불러오면 그 사실을 알린다 — 조용히 넘어가면 안 보이는
      일정이 있는데 '일정 없음' 으로 읽어 약속을 놓친다.
  · 고객정보 서류
      드라이브의 고객 서류 폴더를 앱 안에서 본다. 고객 폴더를 초성으로 찾고,
      눌러 들어가서 그림·PDF 를 그 자리에서 본다. 최근 본 고객 8명이 맨 위에 남는다.
      서류 폴더는 처음 한 번만 정한다(드라이브 앱에서 폴더 링크를 복사해 붙여넣기).
  · 고객관리 조회
      이름·차종·금융사·진행현황으로 찾는다. 초성도 된다("ㄱㅅㅎ" → 김상현).
      진행현황 칩은 시트에 **실제로 있는 값**에서 뽑으므로, 시트에 새 상태를 쓰면
      폰에도 그대로 생긴다.
      밖에서 전화를 받았을 때를 기준으로 만들었다 — 안내멘트에 **큰 복사 단추**
      (카톡에 붙여넣는 게 이 화면을 여는 가장 큰 이유다), 고객센터·사고접수는
      눌러서 바로 걸리는 링크, 견적서 이미지도 상세에서 바로 보인다.
  · 수당계산기
      PC 앱과 **같은 계산식, 같은 수당율 표**(회사 공용 시트의 '수당율' 탭)다.
      차종은 초성으로 찾고, 차량가는 치는 동안 1,000 단위가 붙는다.
      결과 복사에 차종·차량가·수당율·지급율·면세 여부까지 담는다 — 금액만 보내면
      받는 쪽이 무엇으로 계산한 건지 알 수 없다.
      공용 시트를 못 읽어도 앱에 든 기본 표로 계산된다(어느 표를 쓰는지 화면에 적는다).
  · 자료검색
      공용 시트의 업무자료를 초성으로 찾는다. PC 앱과 같은 순위 규칙이다.
  · 정보 — 이 변경 이력을 폰에서 본다.

  오프라인에서도 뜬다. 지하주차장이나 엘리베이터에서 화면이 죽지 않는다.

■ 열 때 자동으로 최신이 된다 — 단 '반쯤' 바뀌지 않게
  · 캐시 이름에 커밋 번호를 넣고 받을 때 통째로 받는다. 한 화면은 언제나 한 세대의
    파일만 본다 — 옛 화면이 새 파일을 섞어 쓰는 일이 없다.
  · 갈아타기를 함부로 하지 않는다. 그게 반쯤 갱신된 앱을 내보내는 대표적인
    방법이다. 방금 켰거나 방금 앞으로 돌아왔을 때만 바꾼다.
  · 호스팅이 10분간 캐시하므로 자동 확인이 늦을 수 있다 → 켤 때 판 번호를
    캐시 없이 한 번 찍어 보고 다르면 곧바로 새 판을 받는다.

■ 폴드7
  · 접힌 커버 화면에서는 아래 탭바, 펼치면 **같은 탭바가 왼쪽 세로 레일**이 되고
    목록과 내용이 두 판으로 갈린다. 기기를 알아내지 않는다 — 접었다 펴도
    새로고침이 없으므로 순수 CSS 라야 한다.
  · 목록과 내용 사이 **경계선을 끌어서** 너비를 정한다(서류·고객·자료 세 탭 모두).
    정한 너비는 탭마다 따로 기억한다. 두 번 누르면 기본값으로 돌아간다.

■ 구글 계정
  · 구글 로그인 한 번으로 시트·드라이브·캘린더·할일을 읽는다.
    로그인이 안 되어도 **화면을 바꾸지 않는다** — 저장해 둔 것으로 계속 쓴다.
  · **계정이 바뀌면 앞사람 자료를 지운다.** 저장한 것은 원래 기기에 딸려 있어서
    다른 계정으로 로그인해도 그대로 남아 보였다(PC 앱은 계정마다 폴더를 따로 쓰는데
    웹에는 그 장치가 없었다). 이제 켤 때 누구인지 **먼저** 확인하고 그린다.
    머리말에 로그인한 계정을 적어 둔다.
  · **설정은 기기가 아니라 계정을 따라다닌다.** 시트 주소와 서류 폴더를 구글 드라이브의
    앱 전용 숨김 폴더에 둔다. PC 앱이 이미 같은 곳에 올리고 있어서,
    **PC 에서 쓰시던 분은 폰에서 아무것도 입력할 필요가 없다.**

■ PC 앱도 같이 바뀌었다
  · **고객관리 시트 주소의 기본값을 없앴다.** 프로그램에 특정인의 시트 주소가 박혀
    있어서, 다른 직원이 자기 계정으로 로그인해도 그 주소가 그대로 채워져 남의 고객
    목록을 열 수 있었다. 이제 [설정] 에서 직접 넣고, 안 넣었으면 조회를 시도조차
    하지 않는다. 이미 쓰고 계신 PC 는 설정에 저장된 값이 있어 그대로 동작한다.
  · 시트를 푸는 계산을 통신에서 떼어 냈다(read_rows → parse_rows). 모바일이 옮겨 쓴
    규칙과 정답표로 대조하기 위해서다.

■ 옮긴 계산은 정답표로 못 박았다
  초성 검색·금액·날짜·시트 해석·수당 계산을 파이썬에서 자바스크립트로 옮기면서,
  파이썬이 뽑은 정답 230건을 자바스크립트가 맞히게 했다. 두 쪽이 조용히 어긋날 수 없다.
  이 대조로 실제 어긋남 8건을 잡았다 — "1.234.567" 을 1.234 로 읽던 것,
  전각 숫자 '３' 을 파이썬만 숫자로 보던 것, 계약개월수가 없을 때 0 과 null 이 갈리던 것.
  수당 계산은 돈이 나오는 자리라 반올림 경계·면세·화물차·지급율까지 17가지를 대조한다.

■ 만들면서 고친 것
  · 펼쳤을 때 제목 '일정관리기' 가 오른쪽에 세로로 서서 화면 절반을 먹던 것.
    가로로 눕힐 때 머리말까지 같은 줄의 칸이 되고 있었다.
  · 그림 위에서 스크롤이 안 되던 것. 두 손가락 확대만 허용하는 설정이
    **한 손가락 움직임을 막고** 있었다.
  · PDF 가 안 뜨던 것. 브라우저 내장 뷰어에 맡겼는데 안드로이드는 있기도 없기도 하다.
    이제 한 장씩 그림으로 그려 아래로 이어 붙인다.
  · 견적서가 상세에 안 나오던 것. 견적서는 =IMAGE() **수식**이라 표시값으로 읽으면
    빈 칸이 와서 '견적서 없음' 과 구분되지 않았다.
  · **카톡으로 링크를 열면 앱이 통째로 안 뜰 수 있던 것.** 브라우저 지원 여부를
    느슨하게 보고 있었는데, 카카오톡 인앱 브라우저는 그 검사를 통과하고도 실제로는
    쓸 수 없다. 카톡으로 주소를 받아 여는 게 가장 흔한 경로라 그냥 둘 수 없었다.
  · 고객 목록이 'HTTP 400' 만 내고 안 뜨던 것. 구글이 왜 거절했는지 알려 주는 말을
    코드가 버리고 있었다. 이제 그 말을 보여 주고, 원인이 대개 탭 이름이므로
    **시트에 실제로 있는 탭 목록**을 받아 눌러 고르게 한다.
  · PC 로 열면 서류가 화면 전체로 늘어나 읽기 어렵던 것.

■ 검사
  브라우저로 실제로 돌려 보는 검사 127가지, 파이썬 정답표 대조 230건.
  서비스워커 세대 교체·오프라인·접고 펴기·계정 갈이·PDF 렌더까지 포함한다.
  배포가 끝나면 **배포된 사이트를 다시 내려받아** 저장소와 한 바이트도 다르지 않은지,
  그 파일들로 앱이 실제로 뜨는지까지 확인한다.

■ 아직 폰에서 확인해야 하는 것 (여기서는 흉내도 못 낸다)
  · 홈화면에 설치한 상태에서 구글 로그인 창이 뜨는지
  · 실제로 접었다 펴기 (웹뷰가 아예 다시 만들어질 수 있다)
  · 삼성 키보드로 초성만 칠 때 기대하는 자모가 오는지
  · PDF 가 폰에서 장마다 제대로 그려지는지

## v1.12.0 — 변경 이력 — 커밋 메시지를 한 곳으로 모으고 앱에서 보여 준다 (2026-09-14)

■ 왜
  릴리스 본문이 계속 비어 있었다(워크플로에 body 가 없었다). 그래서 앱의
  [업데이트 확인] 도 '무엇이 바뀌었나' 를 보여 줄 내용이 없었다. 그런데 이 저장소는
  커밋 메시지를 꼼꼼히 적어 왔고 그게 사실상 변경 이력이었다 — 쓰이지 않고 있었을 뿐이다.

■ 원본 하나로 모았다 — CHANGELOG.md
  태그 67개(v1.1.1 ~ v1.11.0)의 커밋 메시지에서 그대로 뽑아 1,247줄을 만들었다.
  손으로 옮겨 적은 것은 없다.
  · 제목의 버전을 두 형식 다 읽는다 — '제목 v1.4.3' 과 옛 'v1.1.1: 제목'
  · 공동작업자·세션 링크 같은 꼬리표는 떼어 낸다
  · 본문의 ■ 절 관례를 **그대로 살렸다**. 이게 핵심이다 — 업무자료 화면이 쓰는
    body_to_html 을 그대로 재사용해서, 그리는 코드를 새로 만들 필요가 없다.

■ 세 곳이 그 파일 하나를 읽는다
  · PC 앱: [변경 이력] 창(왼쪽 버전 목록, 오른쪽 내용, 지금 쓰는 버전에 표시).
    상단바의 버전을 누르거나 트레이 메뉴에서 연다. exe 안에도 들어간다(spec datas).
  · 릴리스 본문: 워크플로가 그 버전 절을 잘라 넣는다 → 업데이트 확인에도 나온다.
    ⚠️ 절이 없으면 워크플로가 **실패한다** — 변경 이력에 안 적힌 버전은 배포되지 않는다.
  · (다음 작업인 모바일 웹앱도 같은 파일을 읽을 것이다)

■ 같은 내용을 두 번 적지 않게
  tools/release.py --from-head 가 방금 커밋의 메시지를 읽어 CHANGELOG 앞에 붙이고
  APP_VERSION 을 올리고 태그까지 단다. 이 커밋도 그 도구로 만들었다.
  --backfill 은 지난 태그 전부를 한 번에 만든다. --check 는 태그·버전이 맞는지 본다.
  ⚠️ 이미 밀어 둔 커밋이면 amend 를 거절한다 — 밀린 역사는 고치지 않는다.

verify_changelog.py 를 새로 만들었다(25개 항목). 기존 12벌 전부 통과.

## v1.11.0 — 받은 팩스 알람·보기 · 휠 확대 · 표 가독성 · 탭 순서 (2026-09-13)

■ 받은 팩스가 오면 알려 주고 앱에서 바로 본다
  SK텔링크 모바일팩스에는 앱이 붙을 공개 API 도, 수신 팩스 자동 메일 전달도
  없다(확인함). 그래서 폴더를 거친다 — 휴대폰에서 [공유] → 드라이브의 '받은팩스'
  폴더에 저장하면, PC 가 10초 안에 알아채서 알람을 띄우고 [보기] 로 그 자리에서
  읽는다. 손이 가는 곳은 휴대폰에서 공유 한 번뿐이다.

  · 새로 만든 것: fax_watch.py(감시, 화면과 분리) · fax_window.py(보기 창)
  · 이미 있는데 안 쓰던 '조용한 안내 팝업'(siren=False)을 여기서 처음 쓴다.
  · 세 가지 함정을 막았다
    - 반쯤 내려온 파일: 크기가 연속 두 번 같아야 알린다(깨진 PDF 방지)
    - 폴더를 처음 지정할 때: 그때 있던 팩스로는 알람이 안 울린다
    - 같은 팩스 두 번: fax_seen.json 에 남기고, PC 사이에 합집합으로 합친다
      (사무실에서 본 팩스가 집 PC 를 켤 때 다시 뜨지 않는다)
  · 고객정보 탭의 폴더 감시로는 안 된다 — 그건 탭이 보일 때만 돈다. 팩스는 어느
    탭에 있든 트레이에 내려가 있든 잡아야 하므로 창에 딸린 별도 타이머로 뒀다.

■ 팩스 때문에 드러난 미리보기 구멍 두 개 (이게 더 중요하다)
  1) tif/tiff 가 '볼 수 없는 형식' 이었다. 팩스는 TIFF 로 오는 일이 흔한데
     받은 팩스가 안 열렸을 것이다.
  2) **여러 장 그림은 첫 장만 보였다.** 팩스는 여러 장이 보통이라, 2장부터
     조용히 사라졌다 — 서류를 다 받은 줄 알고 넘어가게 되는 종류의 버그다.
     QImageReader 로 장을 세고 넘기게 했다(PDF 와 같은 단추).
     3장짜리 팩스 TIFF 를 실제로 만들어 장마다 다른 그림이 맞게 나오는지 확인했다.

  ⚠️ 그 과정에서 프로그램이 통째로 꺼지는 문제를 두 번 만나 고쳤다. QImageReader 가
     QBuffer 를 가리키고만 있어서, 버퍼가 먼저 없어지면 읽개가 죽을 때 뻗는다
     (한 번은 장을 넘길 때, 한 번은 프로그램을 끝낼 때). 이제 읽개와 버퍼를
     함수 밖으로 내보내지 않고 그 안에서 순서대로 없앤다.

■ 미리보기를 휠로 확대·축소 (Ctrl 없이)
  서류를 볼 때마다 Ctrl 을 같이 누르는 게 번거로웠다. 이제 그냥 굴리면 커지고
  작아진다. 확대한 뒤 자리를 옮기는 것은 끌기로 한다(이미 되던 것).

■ 표 가독성
  일정/할일 표: 글자 11pt · 한 칸 높이 38px (이번주 일정 칸도 그만큼 늘렸다)
  고객관리 표 : 한 칸 높이 40px. 글자는 10pt 그대로 — 열이 많아 키우면 잘린다.

■ 탭 순서를 끌어서 바꿀 수 있다
  자주 쓰는 탭을 앞으로 옮겨 두면 그 순서를 기억한다. 설정에 담으므로 사무실 PC 와
  집 PC 가 같은 배치가 된다. 판이 바뀌어 탭이 늘거나 줄어도 이름으로 맞춰 되돌리고,
  새로 생긴 탭은 눈에 안 띄는 맨 끝으로 밀지 않는다.

verify_fax.py 를 새로 만들었고(41개 항목), 기존 11벌 전부 통과.

⚠️ 윈도에서 확인이 필요한 것: 만들어진 exe 에서 tif 가 열리는지(Qt 그림 플러그인이
   빌드에 들어가는지). 여기서는 리눅스라 흉내만 낼 수 있었다.

## v1.10.1 — 드라이브를 동시에 묻게 된 뒤의 뒷갈망 — 속도제한 대비 (2026-09-11)

1.10.0 에서 드라이브 폴더를 한 겹씩 8개까지 동시에 묻도록 바꿨다. 빨라진 대신
구글이 '너무 빨리 묻는다'(429) 며 막을 여지가 생겼는데, 그때 그냥 넘어가면 그
폴더의 서류가 검색에서 **말없이** 빠진다. 찾는 사람은 서류가 없는 줄로 안다.
내가 만든 위험이라 같이 막아 둔다.

■ 429·5xx 를 만나면 조금 쉬고 다시 묻는다 (최대 3번, 0.6→1.2→2.4초)
  구글이 Retry-After 로 알려 주면 그 값을 따른다. 끝까지 안 되면 조용히
  넘기지 않고 오류로 알린다.

■ 그래도 못 읽은 폴더가 있으면 화면이 말해 준다
  '검색 결과 — 폴더 N곳을 읽지 못해 빠진 서류가 있을 수 있습니다'
  로컬 폴더도 마찬가지로 센다(권한 없는 폴더가 섞일 수 있다).

■ 상한(10만건)까지 훑었으면 그것도 알린다
  '10만건까지만 훑었습니다' — 뒷부분이 검색에 안 나온다는 것을 알 수 있게.
  서류가 그만큼 많아질 일은 드물지만, 조용히 틀린 답을 주지는 않게 한다.

모의 드라이브로 429 두 번 뒤 성공 · 끝까지 바쁘면 오류 · 막힌 폴더 세기를
모두 확인했고 verify_audit.py 에 넣었다. 11벌 전부 통과.

## v1.10.0 — 검색·드라이브 조회 속도 · 미리보기에서 바로 카톡 보내기 (2026-09-11)

■ 검색이 빨라졌다 (재 본 숫자로)
  검색창에 한 자 칠 때마다 파일 전체의 초성을 처음부터 다시 뽑고 있었다.
  그게 검색이 느렸던 진짜 이유다. 이제 세 가지로 나눠 해결했다.

  1) 미리 다듬어 둔다 — 뒤에서 훑을 때 같이 만들어 두고 계속 쓴다.
     2,800건 기준 한 자당 20.8ms → 4.0ms (5배)
  2) 좁혀 가며 찾는다 — '김' → '김상' → '김상현' 처럼 글자를 붙여 갈 때,
     전체를 다시 훑지 않고 직전 결과만 본다.
     10만건 기준 한 자당 135 → 32 → 5 → 1ms (전체 재훑기는 매번 120~173ms)
  3) 타자가 멎으면 찾는다 — 빠르게 치거나 키를 누른 채 둬도 마지막 것만 찾는다.

  결과가 예전과 한 글자도 달라지지 않는 것을 초성·겹자음·영문까지 대조해 확인했다.

■ 드라이브에서 직접 조회할 때 7배 빨라졌다
  폴더를 하나 묻고 답을 기다렸다가 다음 폴더를 묻고 있었다. 왕복 한 번이
  0.2~0.3초인데 고객 폴더가 400개면 그것만으로 100초다. 이제 한 겹의 폴더들을
  한꺼번에(8개씩) 묻는다. 한 번에 받는 개수도 200 → 1000(구글 최대)으로 올렸다.
  모의 드라이브로 6.68초 → 0.92초(7.2배), 결과·폴더경로가 완전히 같음을 확인.
  폴더 하나가 권한 때문에 막혀도 그것만 빠지고 나머지는 다 가져온다.

■ 구글 호출마다 새로 접속하던 것을 고쳤다
  호출마다 TCP+TLS 악수를 다시 했다(사무실 망에서 한 번에 0.1~0.3초).
  연결을 한 번 맺어 두고 다시 쓴다 — 고객관리 불러오기·저장이 전반적으로
  빨라진다. Session 은 스레드마다 따로 둔다(여러 스레드가 같이 쓰면 안 된다).

■ 미리보기에서 바로 카카오톡으로
  서류를 확인한 뒤 다시 목록으로 가서 고르고 Ctrl+C 를 누를 필요가 없다.
  미리보기 단추 줄에 [카톡으로 보내기] 를 두었다. 보낼 수 없는 경우의 안내는
  기존 복사 기능이 그대로 맡는다.

■ 검색 상한을 2만 → 10만으로
  서류가 많은 분도 전부 걸린다. 위 1)2)3) 덕분에 10만건에서도 타자가 밀리지 않는다.

■ 그 밖에 점검 중 찾은 것 두 가지
  · 낡은 훑기가 새 검색 목록을 덮어쓰던 문제
    훑는 데는 시간이 걸린다. 그 사이 파일을 넣으면 새로 훑기 시작하는데,
    먼저 떠난 훑기가 나중에 도착해 새 목록을 낡은 것으로 덮어썼다. 방금 넣은
    서류가 검색에 안 나오는 원인이었다. 폴더 읽기에는 있던 세대 표를 훑기에도 달았다.
  · 동기화돼 들어온 서류가 검색에 안 나오던 문제
    검색 목록을 켤 때 한 번만 만들어서, 하루 종일 띄워 두면 사무실에서 올린
    서류가 검색에 영영 안 나왔다. 폴더가 바뀌면 검색 목록도 다시 만든다.

verify_audit.py 에 위 전부를 못 박았다(모의 드라이브 포함). 11벌 전부 통과.

## v1.9.3 — 전체 코드 점검 — 실제로 재현된 결함 8건 수정 (2026-09-10)

찾아보기만 한 것이 아니라, 하나하나 재현해 놓고 고쳤다.

1. 드라이브 조회 모드에서 검색이 아예 안 됐다
   DriveSource.scan 만 on_batch 를 안 받아, 검색을 켜는 순간 TypeError 가 나고
   '검색 준비 실패' 만 뜬 채 끝났다. 로컬 폴더 쪽과 같은 약속을 지키게 했다.

2. 미리보기에서 Ctrl+휠 확대가 한 번도 먹지 않았다
   마우스는 늘 그림 위에 있는데, 그 휠 알림은 그림표와 스크롤칸이 먼저 받아
   스크롤에 써 버리고 위로 올려 보내지 않았다. 단추 설명에는 'Ctrl + 마우스 휠'
   이라 적혀 있었지만 실제로는 전혀 안 됐다. 이제 그림 위에서도 먹는다.

3. PDF 를 볼 때마다 문서가 통째로 쌓였다
   QPdfDocument 를 화면의 자식으로 붙여 둬서, 다른 파일로 넘어가도 안 없어졌다.
   서류가 많은 폴더를 훑고 나면 메모리가 계속 늘었다. 여덟 장을 넘겨본 뒤
   여덟 개가 남아 있는 것을 확인하고 고쳤다.

4. 파일을 넣거나 지운 뒤 검색 결과가 낡은 채로 남았다
   검색 중이면 새로 읽은 내용을 반영하지 않고 그냥 돌아가 버려서, 검색 목록을
   다시 만들 사람이 아무도 없었다. 넣은 파일이 안 보이고 지운 파일이 계속 보였다.

5. 정렬 기준이 검색으로 넘어가면 뜻이 바뀌었다
   찾아보기는 칸이 3개, 검색은 4개인데 '몇 번째 칸' 으로 기억해서, 날짜순으로
   세워 두고 검색하면 크기순이 됐다. 칸 이름으로 기억하게 했다.

6. 백스페이스가 삭제였다
   탐색기에서 백스페이스는 '상위 폴더로' 다. 위로 올라가려고 누른 사람에게
   지울지 묻는 창이 떴다. 삭제는 딜리트만 남겼다.

7. 모니터를 두 대 쓰면 최대화 세로가 또 줄어들 수 있었다
   창이 어느 모니터에 있든 늘 주 모니터 크기와 견주고 있었다. 보조 모니터
   해상도가 다르면 '최대화인지' 를 잘못 재서 세로를 도로 되돌렸다.
   이제 그 창이 올라가 있는 모니터를 본다.

8. 1초마다 파일 다섯 개를 쓸데없이 두 번씩 들여다봤다
   exists() 도 결국 stat 인데 stat 을 또 불렀고, 그걸 매초 했다(하루 43만 번).
   한 번만 부르고, 5초에 한 번만 보게 했다.

그 밖에: 창을 넓히자마자 끄면 그 폭이 저장되지 않던 것, 검색 결과에서 폴더로
건너뛸 때 위로 올라가는 데 상한이 없던 것, 안 쓰는 import 14개를 정리했다.

verify_audit.py 를 새로 만들어 위 항목을 전부 못 박았고, 기존 10벌도 그대로 통과.

## v1.9.2 — 최대화 세로가 안 커지던 문제 · 확대한 그림 끌어서 보기 (2026-09-10)

■ 최대화하면 가로만 커지고 세로는 그대로이던 문제
  원인은 우리 쪽 '되돌리기' 였다. 크기는 '보통' 과 '최대화' 둘뿐이어야 하므로
  보통 상태에서 세로가 달라지면 도로 되돌리는데, 최대화를 누르면 '커졌다'
  (resize) 가 '최대화됨'(state) 보다 **먼저** 오는 창관리자가 있다(윈도).
  그 틈에는 아직 보통으로 보이므로 우리가 세로를 도로 920 으로 되돌려 버렸다.
  가로는 제약이 없어 커진 채 남고 — 그래서 가로만 최대화된 것처럼 보였다.

  이제 상태 비트만 믿지 않고 크기도 함께 본다. 창이 '화면만큼' 커져 있으면
  상태 비트가 아직 안 켜졌어도 최대화로 친다. 화면보다 훌쩍 큰 창은 최대화가
  아니라 그냥 억지로 키운 것이므로 되돌린다(범위로 견준다).
  되돌리기 판단 자체도 한 박자 미뤄 상태 알림이 처리될 틈을 준다.

  실제 창관리자(Xvfb+openbox)에서 1570x920 → 1920x1181(가로·세로 모두 꽉)
  → 1570x920 을 확인했고, 보통 상태에서 세로를 끌면 여전히 되돌아온다.

■ 확대한 그림을 끌어서 옮겨 보기
  확대하면 그림이 칸보다 커진다. 이제 스크롤막대를 잡는 대신 그림을 직접
  끌어서 보고 싶은 자리로 옮길 수 있다(지도 앱과 같은 조작). 끌 수 있을 때는
  손 모양 커서로 알려 주고, 맞춤 상태에서는 끌어도 아무 일이 없다.

## v1.9.1 — 고객정보 — 파일을 복사해서 / 끌어서 카카오톡으로 (2026-09-10)

파일을 고르고 Ctrl+C 하면 클립보드에 올라가고, 카카오톡 대화창에서 Ctrl+V
하면 붙는다. 목록에서 그대로 끌어다 대화창에 놓아도 전송된다.
우클릭 메뉴에도 [복사 (Ctrl+C)] 를 넣었다.

클립보드·드래그에는 '파일' 을 싣는다(파일 목록/urls). 그림 데이터만 실으면
받는 쪽이 이름도 원본 화질도 모른 채 다시 만들어 내므로, 파일이 본체다.
경로 글자와, 그림 파일일 때는 그림 데이터도 함께 실어 둔다 — 파일을 못 받고
그림만 받는 곳(한글·워드 등)을 위해서다. PDF 는 파일로만 실린다.

목록에서 끌어다 '같은 폴더' 에 도로 놓으면 아무 일도 하지 않는다. 안 그러면
그럴 때마다 '이름 (2)' 사본이 생긴다(끌어내기를 켜면서 생긴 길이다).

드라이브를 API 로 조회하는 중에는 PC 에 파일이 없으므로 복사·끌어내기가
안 된다는 것을 알려 준다.

## v1.9.0 — 최대화 제대로 · 목록 정렬 · 미리보기 확대 (2026-09-09)

■ 최대화 — 이번엔 진짜 창관리자로 확인했다
  이 컨테이너에 Xvfb + openbox 를 올려서, 제약 없는 맨 창과 견주며 실제로
  최대화해 봤다. 그랬더니 남아 있던 원인이 드러났다 — 가로 상한이었다.
  setMaximumWidth(화면폭-20) 이 걸려 있으면 창관리자는 그 값까지만 키운다.
  실험에서 상한 900 을 걸어 두니 최대화가 900x1181 에서 멈췄다. 창관리자는
  이 값으로 '이 창이 얼마나 커질 수 있는가' 를 판단하므로, 화면보다 작은
  상한은 최대화를 반쪽으로 만들거나 단추 자체를 죽인다.
  그래서 가로 상한을 아예 걸지 않는다. 화면 밖으로 나가지 않게 하는 일은
  창관리자가 알아서 한다. 이제 1570x920 → 1920x1181(화면 꽉) → 1570x920 이
  실제 창관리자에서 확인된다.

■ 목록 줄 세우기
  이름·크기·날짜(검색 중엔 위치) 머리글을 누르면 그 기준으로 세운다. 같은
  머리글을 또 누르면 거꾸로. 지금 기준은 머리글의 ▲▼ 로 보여 준다.
  폴더는 어느 기준에서도 늘 위에 둔다(윈도 탐색기와 같다). '상위 폴더로' 줄은
  언제나 맨 위.

■ 미리보기 확대
  [－][＋][맞춤] 과 Ctrl+휠, 두 번 누르기(맞춤 ↔ 100%)로 키워 본다. 확대하면
  스크롤이 생겨 원하는 자리를 볼 수 있다. 배율은 옆에 % 로 보여 준다.
  확대할 때 보고 있던 한가운데를 그대로 지킨다 — 안 그러면 왼쪽 위 구석으로
  튀어 보던 자리가 사라진다.
  PDF 는 늘려 그리지 않고 그 배율로 '다시' 그린다. 그래야 글자가 안 뭉개진다.
  맞춰 보는 동안에는 스크롤막대를 꺼 둔다. 켜 두면 막대가 생겼다 사라지며
  칸 크기가 흔들려 맞춤 크기가 그때그때 달라졌다.

## v1.8.0 (2026-09-09)

앞 커밋(최대화 수정 · 고객정보 파일 넣고 빼기 · 검색 속도)을 배포한다.

## v1.7.0 — 미리보기 돌리기 · PDF 미리보기 · 창 크기 두 가지 (2026-09-09)

1. 그림 돌려 보기
   미리보기에 [↺ 왼쪽] [↻ 오른쪽] 을 붙였다. 스캔한 서류는 눕혀 찍힌 게
   흔해서 필요하다. 돌린 각도는 파일마다 따로 기억하므로 목록을 오갔다가
   돌아와도 그 각도 그대로다.

2. PDF 미리보기
   Qt 가 페이지를 그려 준다(QtPdf). 여러 장이면 [◀] [▶] 로 넘기고 '2 / 3'
   처럼 몇 장 중 몇 장인지 보여 준다. PDF 도 돌려 볼 수 있다.
   종이는 흰색으로 깔았다 — 그냥 그리면 글자만 있고 배경이 비어서, 어두운
   화면에서는 검은 바탕에 검은 글씨가 되어 아무것도 안 보였다.
   빌드 설정에서 QtPdf 를 빼 두고 있었으므로 그 제외도 풀었다(쓰는 데가
   없어서 용량 줄이려고 빼 뒀던 것이다).

3. 창 크기는 '보통' 과 '최대화' 둘뿐
   최대화 단추를 살렸다. 보통일 때는 예전처럼 세로 고정 · 가로만 조절이고,
   최대화하는 순간 그 세로 고정을 잠시 풀어 화면을 꽉 채운다. 되돌리면 다시
   고정하고 쓰던 폭으로 돌아간다 — 그래서 어중간한 세로 크기는 나올 수 없다.
   최대화로 쓰던 사람은 다음에 켤 때도 최대화로 열린다. 최대화 중에는 그때의
   폭을 '보통 폭' 으로 적지 않는다(적으면 최대화를 풀었을 때 그 폭이 굳는다).

## v1.6.0 — 고객정보 — 드라이브처럼 폴더를 눌러서 들어간다 (2026-09-09)

1. 첫 화면이 바로 뜬다
   예전엔 켤 때마다 트리 전체를 훑어(scan) 그 목록을 통째로 보여 줬다.
   드라이브 폴더가 크면 첫 화면이 나오기까지 한참 멈춰 있었다.
   이제 화면은 폴더 '한 겹' 만 읽는다(children).
   고객 400명·서류 2400장으로 재 보니 한 겹 1.7ms / 전체 19.2ms 였고,
   탭을 열어 첫 목록이 나오기까지 73ms 였다.

2. 드라이브와 같은 구조
   맨 위에는 상위 폴더만 나오고, 폴더를 두 번 누르면 그 안으로 들어간다.
   지금 어디인지는 위에 '고객정보 / 2026-08 김상현(신한카드)' 처럼 보여 준다.

3. 새로고침을 안 눌러도 반영된다
   지금 보고 있는 폴더를 잠깐씩 다시 읽어(로컬 5초, 드라이브 45초) 요약값이
   달라졌을 때만 조용히 갱신한다. 고른 줄은 그대로 둔다. 창이 안 보이거나
   검색 중이면 건너뛴다 — 화면을 괜히 흔들지 않는다.

4. 상위 폴더로 돌아가기
   하위 폴더에서는 목록 첫 줄이 '⬆ .. (상위 폴더로)' 다. 위쪽 [⬆ 상위 폴더]
   단추로도 올라간다. 창고 밖으로는 못 올라간다.

검색은 트리 전체를 봐야 하므로 뒤에서 조용히 훑어 둔다. 훑는 동안에도 폴더를
눌러 다니는 데는 지장이 없다. 검색 중에는 '위치' 열이 생겨 어느 폴더의
파일인지 보이고, 검색 결과에서 폴더를 누르면 그 폴더로 옮겨간다.

표에서 '이름' 이 남는 폭을 받는다. 마지막 칸을 늘리던 예전 설정으로는 날짜
칸이 허옇게 넓어지고 정작 긴 폴더 이름이 잘렸다.

## v1.5.1 — 고객정보 탭이 지정한 폴더를 못 읽던 문제 (2026-09-09)

폴더를 지정해도 늘 0건이고 [새로고침] 이 잠긴 채로 남았다.

원인 — 탭이 설정을 '복사해' 들고 있었다.
로그인해서 계정이 확인되면 메인 창이 reload_data() 로 설정 객체를 통째로
새로 읽는다(self.settings = AppSettings.load(...)). 고객정보 탭은 만들 때
받은 옛 객체를 계속 붙들고 있어서, 설정에서 폴더를 지정하면 그 값은 새
객체에만 들어가고 탭은 빈 값을 보았다. 그래서 폴더를 아무리 지정해도
'폴더가 지정되지 않았습니다' 상태였다.

고친 것
- 탭은 이제 창이 지금 쓰는 설정을 직접 본다(property). reload_data() 에서도
  새 객체를 넘겨 준다.
- 설정에서 서류 폴더를 바꾸면 그 자리에서 다시 읽는다. 예전엔 프로그램을
  다시 켜야 했다.
- 훑는 중이어도 [새로고침] 이 다시 눌린다. 예전엔 _scanning 이 True 로 남으면
  영영 잠겼다. 훑기가 터져도 반드시 풀리도록 결과를 한 번은 보낸다.
- 왜 안 되는지 화면에 적는다(툴팁은 아무도 안 본다) — 읽을 곳 없음 / 읽기
  실패 / '0건 — 폴더가 비어 있습니다'. 폴더는 찾았는데 빈 경우와 폴더를
  못 찾은 경우를 구분해서 보여 준다.
- 늦게 끝난 예전 훑기가 새 결과를 덮어쓰지 않게 세대 번호를 붙였다.

## v1.5.0 — '고객정보' 탭 — 고객별 서류 찾아보기 (2026-09-09)

메인 화면에서 바로 로그인할 수 있게 하고(앞 커밋), 고객별 서류를 앱 안에서
찾아볼 수 있는 탭을 새로 만들었다.

■ 고객정보 탭
  폴더명·파일명으로 찾고(초성도 된다), 고른 그림은 오른쪽에 바로 보여 준다.
  고객 이름으로 찾으면 그 고객 폴더 안의 서류까지 함께 걸린다 — 폴더를 하나씩
  열어 볼 이유가 없기 때문이다. 고객관리에서 우클릭 → '고객정보에서 … 서류
  보기' 로 바로 건너뛸 수도 있다.
  폴더가 크면 훑는 데 몇 초 걸리므로 켤 때가 아니라 탭을 처음 열 때 읽는다.

■ 자료를 어디서 읽는가 — 로컬 폴더가 기본, 드라이브 API 는 켠 사람만
  구글 드라이브 데스크톱이 PC 에 내려받아 둔 폴더를 그냥 읽는다. 사무실과 집
  양쪽에 드라이브 데스크톱이 깔려 있으면 한쪽에서 넣은 서류가 다른 쪽에서도
  보인다. 추가 권한도, 인터넷도 필요 없다.

  드라이브를 직접 조회하려면 drive.readonly 권한이 필요한데, 구글이
  'restricted' 로 묶어 둔 권한이라 기본 목록에 넣으면 **모든 사용자**가 로그인
  때 '확인되지 않은 앱' 경고를 보게 되고, 배포하려면 앱 인증과 매년 유료
  보안심사(CASA)를 통과해야 한다. 그래서 기본에서 빼고, 설정에서 켠 사람만
  로그인 때 함께 요청하도록 authorize(extra_scopes=...) 를 열어 두었다.
  안 켜도 기능은 그대로 쓸 수 있다.

## v1.4.4 — 고객관리 글씨 한 단계 축소 · 열 폭과 창 폭 조정 (2026-09-08)

1. 고객 목록 글씨 11pt → 10pt (기본 글씨와 같은 크기).
2. 알려 주신 값으로 폭을 잡는다.
   고객명 236 · 내용 255 · 안내멘트 80 · 창 가로 1570.

안내멘트 80 은 복사 단추 때문이다. 복사하면 글자가 '복사' → '✓ 복사' 로
길어지는데, 68 일 때 체크표시가 잘렸다. 실제 글꼴로 재 보니 '✓ 복사' 가
73px 라 80 이면 들어간다.

검사 쪽 구멍도 하나 메웠다. verify_reg 가 앱과 같은 글꼴(Malgun Gothic 10
DemiBold)을 안 심고 Qt 기본 글꼴로 재고 있었다. 그래서 열 폭·단추 폭이
실제보다 넓게 나왔다. 이제 app.py 와 같은 글꼴로 재므로 여기서 잰 값이
실제 화면과 맞는다.

## v1.4.3 — 남색기 뺀 다크 · 고객 목록 넓히기 · 계약조건 수기 입력 (2026-09-07)

1. 다크를 완전한 무채색으로. R=G=B 라 남색기가 없다(#0A0A0A/#141414).
2. 고객관리 버튼 줄에서 [서류] 제거. 서류·이력은 우클릭 메뉴에 남겨 두었다
   — 이미 적어 둔 상담 메모를 못 열게 되면 안 되므로 지우지는 않았다.
3. 시작할 때 '미출고차량' 을 조회하던 문제. 계정이 확인되기 전에는 공용
   폴더의 기본 설정을 들고 있어서, 설정에 적어 둔 시트 이름 대신 기본값으로
   조회해 오류창이 떴다. 게다가 한 번 시도한 걸로 표시돼 계정 확인 뒤에도
   다시 불러오지 못했다. 구글에 연결돼 있으면 계정 확인을 기다린다.
4. 테마를 기본·따뜻한 크림·다크 셋만 남기고, 예전 이름은 가까운 테마로 옮긴다.
5. 이력 줄 높이·글씨 키움(12pt).
6. 고객 목록 글씨 11pt, 줄 높이 34px, 열 폭을 실제로 재서 다시 잡음.
7. 계약조건은 수기 입력만 — 드롭다운 삭제, 폼 글씨 12pt.
8. 차종 뒤 '차량가격', 수수료 뒤 '내용' 열 추가.
9. 상단 요약표를 크고 굵게, 패널을 깔아 한 덩어리로.
10. 자료검색 좌측 목록 12pt.

열 폭은 눈대중이 아니라 11pt 로 실제 그려 sizeHintForColumn 으로 쟀다.
글씨만 키우고 예전 폭을 두었더니 금융사·차종·차량가격·계약일·출고일·수수료
여섯 열이 잘렸다. 잘려도 되는 건 '내용' 뿐이라 잰 값에 맞춰 다시 잡고,
창 폭 상한을 1280→1400 으로 올려 '내용' 에 200px 를 남겼다. 화면이 좁으면
그대로 화면 폭으로 깎이므로 작은 노트북에서도 안전하다.

## v1.4.2 — 다크 더 어둡게 · 굵은 글자 · 브리핑 보드 버튼 하나로 (2026-09-07)

1) 다크 어둡기를 브리핑 팝업의 글 영역(#101216)에 맞췄다
   패널을 #1B1E24 → #101216 으로, 바탕을 #050608 로 내렸다.
   이 정도로 어두워지면 배경끼리의 밝기 차가 물리적으로 압축된다 —
   #101216 과 1.12 차이를 내려면 바탕이 순검정이어야 한다. 그래서 면 구분은
   밝기 대신 **테두리**가 맡도록 border 를 패널 대비 1.33 으로 올렸다.
   글자 대비는 그대로 본문 7:1(17:1 나옴), 나머지 4.5:1 을 지킨다.
   ※ '미드나잇'(다크 보라)은 손대지 않았다 — 고르실 수 있게 남겨 둔다.

2) 기본 글자를 굵게
   맑은 고딕은 Regular 와 Bold 두 벌뿐이라 600 이상이어야 실제로 Bold 로
   그려진다. 스타일시트(QWidget)와 앱 기본 폰트(QFont.DemiBold) 양쪽에
   넣었다 — 스타일시트가 닿지 않는 기본 대화상자·툴팁·표 안 글자까지 같은
   두께로 나오게 하기 위해서다.

3) 브리핑 보드를 버튼 하나로
   - [일정 / 할일] 탭 [새로고침] 옆에 [주간 요약] 을 두고, 켤 때 나오는 그
     화면(오늘 브리핑 + 주간 요약)을 그대로 연다.
   - 고객관리 탭의 [주간 요약] 을 없앴다.
   - 트레이 메뉴의 [오늘 브리핑] + [주간 요약] 두 줄도 한 줄로 합쳤다.
   - 따로 열던 주간 요약 창(weekly_dialog.py)과 브리핑만 띄우던
     show_briefing() 은 부르는 곳이 없어져 지웠다. 다만 그 창에만 있던
     **주 이동(← 지난 주 / 다음 주 →)은 보드 안으로 옮겨** 기능이 줄지 않게
     했다. 복사·저장도 보고 있는 주를 따른다.

## v1.4.1 — 브리핑 색 언어를 앱 전체로 (2026-09-07)

브리핑은 theme.strong() 의 강조색 6가지를 써서 테마마다 알맞은 밝기로 나오는데,
나머지 화면은 색이 코드에 박혀 있어 테마를 따라가지 못했다. 재 보니 실제로
안 읽히는 자리가 여럿이었다.

  상단 현황 '발주'      밝은 화면 2.48:1
  고객이력 '만기'       밝은 화면 2.48:1
  고객이력 '안내'       밝은 3.88 / 다크 3.97
  상단 현황 '가망고객'   다크 3.21:1
  캘린더 토·일요일       다크 3.59 / 3.85
  구글 연결 상태 라벨    다크 2.84:1
  멘트복사 [부재중]      흰 글자 3.74:1  ← 아홉 테마 전부
  멘트복사 [복사됨]      흰 글자 3.30:1  ← 아홉 테마 전부

전부 브리핑과 같은 색 이름(blue·green·red·orange·violet·gray)을 쓰도록 바꿨다.
이제 아홉 테마 모두 4.5:1 을 넘는다.

- 색을 채운 단추(멘트복사)는 strong() 을 못 쓴다 — 그건 '패널 위 글자색' 이라
  밝아서, 그 위에 흰 글자를 올리면 3:1 대로 떨어진다. 채움색은 반대로 진해야
  해서 theme.fill()/fill_hover() 를 따로 뒀다. 테마와 무관하게 한 벌인 이유도
  같다(흰 글자가 어느 테마에서든 읽혀야 한다).
  빨강·보라는 넣지 않았다 — 두 조건을 같이 만족하는 폭이 4.50 언저리로 아주
  좁아 조금만 손봐도 깨지고, 지금 쓰는 곳도 없다.
- 밝은 테마의 강조색도 배지 위에서 3.5~4.5 밖에 안 나와 함께 손봤다.
  주황만 한 단계 낮추고, 배지 배경을 조금 밝혔다.
- 멘트복사 단추가 테마 변경을 따라가지 않던 것도 고쳤다 — 카드의 apply_theme
  이 단추까지 내려가지 않아, 테마를 바꿔도 예전 색으로 남아 있었다.
- app.py 에 쓰이지 않는 스타일시트 한 벌(90줄)이 옛 색을 그대로 담은 채 남아
  있어 지웠다. 화면 색은 이제 theme.py 한 곳에서만 만든다.

검증에 '화면 코드에 색이 박혀 있지 않다' 를 넣어 두어, 다음에 또 박아 넣으면
바로 걸린다.

## v1.4.0 — 다크 테마를 금융앱 톤으로 — 파란 포인트 + 글자 시인성 (2026-09-07)

예전 다크는 본문 글자가 #D4DAE1(패널 대비 11.9:1)이라 오래 보면 흐릿했고,
포인트색이 흐린 하늘색(#6FA8DC)이라 어디가 눌리는 자리인지 잘 안 보였다.

- 글자를 #F2F4F6 으로 올렸다(15.1:1, AAA). 대신 바탕을 #121417 → #0E1013 으로
  더 내려 눈부시지 않게 한다. 패널·입력칸·표머리는 서로 1.12 이상 밝기 차이를
  둬서 층이 보이게 했다.
- 포인트를 밝은 파랑(#4593FC)으로. 선택 행은 회색이 아니라 파란 기가 도는
  #1E3A5F, 캘린더 칩1 도 파랑 계열로 맞췄다.
- 브리핑 강조색 6가지를 다크용으로 다시 잡았다(패널 대비 6:1 이상).
- 고른 탭 위에 포인트색 2px 띠를 얹었다. 글자색만으로는 어느 탭인지 약했다.
  테마마다 자기 accent 를 쓰므로 밝은 테마에서도 어색하지 않다.
- 기본 동작 단추(저장·확인)의 테두리를 2px 로. 안을 채우지 않은 이유는
  밝은 파랑 위의 흰 글자가 3:1 대로 떨어져 되레 읽기 나빠지기 때문이다.

그 과정에서 다크에서만 안 읽히던 자리를 하나 찾아 고쳤다 — 고객표의 진행현황
색이 코드에 박혀 있어(#2F6FD0 등) 어두운 화면에서 대비가 3.42:1 까지 떨어졌다.
테마의 강조색을 쓰도록 바꿔 9가지 테마 모두 4.5:1 을 넘는다(다크는 6.29:1).
'진행보류' 도 회색으로 함께 넣었다.

색은 눈으로 고르지 않고 대비를 재서 맞췄다. 검증에 본문 7:1 / 나머지 글자
4.5:1 / 배경끼리 1.12 를 기준으로 넣어 두었다(verify_dark).

## v1.3.9 — 팔로업 제목 바꾸기 · 설정창 넓히기 · 야간정리 앱스크립트 (2026-09-06)

1) 팔로업 제목을 "[출고 1개월] 홍길동" 으로
   "[팔로업] 홍길동 (출고 1개월)" 에서 '팔로업' 이라는 말을 뺐다.
   캘린더에서 제목 앞부분만 보일 때 무슨 건인지 바로 알 수 있다.
   - 제목은 followup.make_title() 한 곳에서만 만든다.
   - 이미 쌓인 할일과 **구글 캘린더 일정도 함께 고친다.** 켤 때 한 번
     훑어서 옛 제목이 있으면 바꾸고, 없으면 그냥 지나간다.
   - 개월 수와 키워드는 **제목에 적힌 값**을 쓴다. 설정을 1→3개월로 바꿨다고
     예전에 만든 1개월 건까지 3개월로 둔갑시키면 안 된다.
   - 이름 꺼내기는 새 모양·옛 모양을 둘 다 읽는다. 옛 제목 할일이 남아 있어도
     중복 판정이 계속 걸린다.
   - 캘린더는 고칠 수 있는 것만 건드린다(cal_id/event_id 가 있는 것).
     읽기 전용으로 구독한 캘린더는 손대지 않는다.

2) 설정 창을 30% 넓게 (560→728 최소 / 620→806 기본)
   안내문이 여러 줄이라 좁으면 줄이 자꾸 접혔다.
   화면이 좁으면 화면 밖으로 나가지 않게 거기서 멈춘다.

3) 야간정리 앱스크립트 (appsscript/야간정리.gs)
   PC 가 꺼져 있어도 매일 새벽에 혼자 도는 스크립트.
   - [만기예정] 계약조건의 '60개월' 표기로 만기일을 계산해 6개월 이내 건을
     급한 순으로 모은다.
   - [정체건] 진행현황이 14일 이상 그대로인 건을 오래된 순으로 모은다.
     시트에는 '언제 바뀌었는지' 가 없어서, 스크립트가 [_정리기록] 숨김 탭에
     어제 값을 적어 두고 비교한다. 기록이 없는 첫날은 계약일부터 센다
     — 없는 날짜를 지어내지 않는다.
   - 원본 시트는 읽기만 한다. 만드는 탭 3개 밖으로 나가지 않는다.
   - 넣는 법과 트리거 설정을 파일 맨 위에 적어 두었다.

검증: 앱스크립트는 구글 전역만 가짜로 채우고 실제 코드를 node 로 그대로
돌렸다(verify_gs.js, 60여 항목). '원본을 한 번도 쓰지 않는다' 와 '두 번 돌려도
탭이 늘지 않는다' 를 포함한다.

'출고일이 적히면 캘린더에 자동 등록' 은 이미 시트 앱스크립트가 하고 있어
앱에는 넣지 않았다.

## v1.3.8 — 시작 화면 합치기 · 주간 요약 개편 · 등록폼 기본값 · 팔로업 2주치 (2026-09-06)

1) 시작 화면 — 브리핑 [확인] 이 안 눌리던 문제
   브리핑은 '항상 위' 로 뜨는 알람 팝업이고 주간 요약은 모달 창이었다.
   모달이 입력을 전부 가로채서, 위에 보이는 브리핑은 안 눌리고 뒤에 깔린
   주간 요약의 [확인] 만 눌렸다. 두 화면을 한 창에 좌우로 합쳤다
   (startup_dialog.py). 좁은 화면에서는 위아래로 접힌다.
   [브리핑 복사] · [주간 요약 복사] · [주간 요약 저장] 을 한자리에 뒀다.
   요일을 골라 한 번만 띄우던 설정은 켜기/끄기 하나로 줄였다 —
   이제 매일 브리핑과 함께 나오므로 요일이 의미가 없다.

2) 주간 요약
   - 금융사별, 다음 주 출고 예정을 뺐다.
   - 진행 중(출고 대기)에서 '진행보류' 를 뺀다. 내가 재촉할 건이 아니라
     기다리는 건이라 목록에 섞이면 급한 건이 묻힌다.
   - '다음 주 만기' 를 '6개월 이내 만기 예정' 으로 바꿨다. 한 주씩 끊어
     보면 미리 연락해 둘 여유가 없다. D-day 순으로 세운다.
     브리핑의 만기 알림 설정(코앞의 건, 기본 3개월)과는 따로 둔다.

3) 고객 등록 창 기본값
   - 계약일은 미정 해제 + 오늘 날짜, 출고일은 미정. 등록할 때 늘 하던
     손질이라 기본값으로 옮겼다.
     ⚠️ 수정 창에는 적용하지 않는다 — 비어 있던 계약일이 창을 열었다
     저장하기만 해도 오늘 날짜로 채워진다.
   - 특판/대리점, 진행현황(출고·취소·발주·진행보류)을 시트에 그 값이
     아직 없어도 항상 고를 수 있게 했다.
   - 계약조건을 금융사별로 가르지 않는다. 금융사를 고르기 전에는 목록이
     비어 있었고, 같은 조건을 여러 금융사에 쓰는데도 금융사마다 또 적어
     둬야 보였다. 이제 설정에 적어 둔 것이 처음부터 전부 나온다.

4) 팔로업
   - 앞으로 2주치를 미리 올린다. 예전엔 그날이 되어야 할일이 생겨서
     아침에야 '오늘 전화할 곳' 을 알았다. 주 단위로 묶어 돌릴 수 있게 됐다.
   - 미리 올리는 만큼 같은 고객이 두 줄로 서지 않게 이름으로도 막는다.
     한 고객의 출고 일정이 캘린더에 두 번 들어가 있거나 차를 두 대 뽑은
     경우, 일정 UID 로만 막으면 이름이 같은 팔로업이 두 개 생겼다.
     같은 이름이면 빠른 쪽을 남기고, 건너뛴 일정은 처리 완료로 적어 둔다.

검증 하니스는 컨테이너가 새로 뜨면서 사라져 다시 만들었다(verify_138 ·
verify_reg). offscreen 화면은 800px 라 창 크기를 시험할 수 없어 vnc 플랫폼에
1600x1000 을 지정해 돌린다.

## v1.3.7 — 창 가로 폭을 끌어서 조절 (2026-09-01)

지금까지 창은 setFixedSize 로 가로·세로가 모두 잠겨 있었다. 넓히고 싶은 쪽은
늘 가로였다 — 고객관리 표가 넓을수록 고객명·차종이 안 잘린다.

- 세로만 고정하고(setFixedHeight) 가로는 열어 둔다.
  세로를 풀면 '이번주 일정'과 '내 할일' 비율이 틀어지고 브리핑 칸이 허옇게
  남는다. 최대화 버튼은 계속 감춘다 — 최대화는 세로까지 바꾸려 든다.
- 최소 860px, 최대는 화면 폭. 860 아래로는 어느 탭이든 버튼 줄이 겹친다.
  좁히면 고객표에 가로 스크롤이 생기고, 넓히면 남는 폭을 고객명 열이
  가져간다(_fit_cust_columns 가 이미 하던 일).
- 맞춰 둔 폭은 기억해 다음에 그대로 연다. 끄는 동안 계속 쓰지 않도록
  멈춘 뒤 한 번만 저장한다.
- 폭은 공용 폴더(window.json)에 둔다. 계정 폴더는 로그인해야 정해지는데
  창은 그 전에 이미 떠 있고, 그 폴더는 다른 PC 로 동기화까지 된다.
  모니터가 작은 PC 가 화면에 맞춰 줄인 폭이 큰 모니터 PC 로 넘어가면
  켤 때마다 서로 창을 좁히게 된다.
- 깨진 파일·화면 밖 값이 들어와도 화면 안 값으로 되돌린다.

검증은 offscreen 화면이 800px 라 최소·최대가 같은 값으로 뭉개져 폭 조절
자체를 시험할 수 없었다. vnc 플랫폼에 크기를 지정해(1600x1000) 실제 모니터에
가까운 화면에서 돌렸다.

## v1.3.6 — 고객 연락처 바로쓰기 · 주간 요약 (2026-08-30)

C) 고객관리 표를 우클릭하면 그 고객 금융사의 번호가 바로 나온다
   - 번호는 시트 P·Q(고객센터·사고접수)를 먼저 쓴다. 그 시트를 직접
     관리하시는 값이라 가장 믿을 만하다. A~Q 는 이미 받아 오고 있어
     요청이 늘지 않는다.
   - 시트가 비어 있으면 자료검색에 넣어 둔 캐피탈 자료에서 찾는다.
   - 이름이 애매하면 하나를 고르지 않고 후보를 하위 메뉴로 보여 준다.
     '우리캐피탈' 은 우리금융캐피탈·JB우리캐피탈·우리카드 셋 다 될 수 있다.
     임의로 고르면 남의 캐피탈로 전화를 걸게 된다. 알맹이(캐피탈·카드·금융
     같은 꼬리말을 뗀 이름)로 비교해 셋을 다 후보로 잡는다.
   - 본문 아무 데나 있는 숫자를 줍지 않는다. '■ 연락처'·'■ 사고접수'
     아래의 '· 이름 : 번호' 꼴만 본다 — 안 그러면 승계 수수료 165,000원이
     전화번호로 둔갑한다.
   - 메뉴에서 자료검색 탭으로 바로 넘어갈 수 있다. 넘어갈 때 분류 거르개는
     전체로 되돌린다(남아 있으면 0건만 뜨고 이유를 알 수 없다).

E) 주간 요약 — 이번 주 출고·계약·수수료와 다음 주 예정을 한 장으로
   - 이번 주 출고(수수료 합계) / 이번 주 계약 / 금융사별 / 다음 주 출고 예정 /
     진행 중(출고 대기, 오래 묵은 순) / 다음 주 만기.
   - 취소 건은 어디에도 넣지 않는다. 한 주는 월요일 시작이다.
   - 지난 주로 오갈 수 있고, [복사]·[파일로 저장]이 있다.
   - 정한 요일(기본 금요일)에 앱을 켜면 한 번 뜬다. '이번 주 것을 봤는가' 를
     주 단위로 기억해, 그날 쉬어도 그 주에 한 번은 보게 된다.
   - 브리핑처럼 내용을 먼저 만들고 평문·HTML 두 가지로 그린다.

그 과정에서 회귀 하나를 막았다 — 새 메서드 이름을 open_kb_search 로 지어
전역 단축키가 부르던 같은 이름의 빠른검색 창을 덮어쓸 뻔했다(v1.2.5 와 같은
사고). open_kb_for 로 바꾸고 중복 이름 검사에 걸리는 것을 확인했다.

## v1.3.5 — 밝은 테마 4가지 · 고객 더블클릭은 수정 · 분류 콤보 글씨 잘림 (2026-08-30)

- 테마: 아이보리 / 살구 / 세이지 / 민트 추가. 모두 순백을 피한 밝은 톤이고
  글자 대비는 WCAG AA(4.5:1) 이상으로 맞췄다.
- 고객관리: 표를 더블클릭하면 [수정] 이 열린다(수정이 이력보다 훨씬 잦다).
  버튼 줄의 [수정] 자리는 [이력] 이 가져간다.
- 자료검색: 검색형 콤보는 안에 입력칸이 있어 Qt 가 재는 폭이 글씨보다 좁다.
  항목이 바뀔 때마다 직접 재서 넓히고(150~280px), 검색칸이 남는 폭을 가져간다.
- v1.3.4(링크를 크롬으로 열기)를 되돌렸다.

## v1.3.4 — 링크를 크롬으로 열기 (2026-08-29)

윈도우 기본 브라우저가 엣지로 잡혀 있으면 자료검색의 링크나 [시트 열기] 가
평소 안 쓰는 브라우저로 떴다. 로그인 상태도 달라 매번 다시 로그인해야 했다.

- browser.open_url() 하나를 두고, 크롬이 깔려 있으면 크롬으로 연다.
  경로는 레지스트리(App Paths)를 먼저 보고, 없으면 표준 설치 경로를 찾는다.
  관리자 권한 없이 설치한 %LOCALAPPDATA% 경로까지 본다.
- 크롬이 없거나 실행이 실패하면 예전처럼 기본 브라우저로 넘긴다.
  링크가 안 열리는 상황은 만들지 않는다.
- 자료검색 본문은 Qt 에 맡기지 않고(setOpenExternalLinks(False)) 직접 넘긴다.
  이때 setOpenLinks(False) 를 같이 꺼야 한다 — 안 그러면 QTextBrowser 가 그
  주소를 제 문서로 열어 본문이 사라진다. 회귀 검증에 그 경우를 넣었다.
- 시트 열기 · 구글 캘린더 열기 · 견적서 브라우저로 열기도 같은 통로를 쓴다.
- 구글 로그인만 기본 브라우저 그대로 뒀다. 크롬과 기본 브라우저에 로그인된
  구글 계정이 다를 수 있어, 로그인 흐름까지 바꾸면 엉뚱한 계정이 뜬다.

## v1.3.3 — 동기화: 파일별 비교 + 합치기 — 다른 PC 수정이 지워지던 문제 (2026-08-29)

PC A 에서 멘트 문구에 채널을 추가했는데 PC B 에 안 나온다는 제보.
동기화 설계에 결함이 둘 있었고, 둘 다 다른 PC 의 수정을 지워 버린다.

1) 켜기만 해도 그 PC 의 파일이 클라우드를 덮어썼다.
   push 가 이 PC 의 묶음을 그대로 올렸다. 이제 원격을 먼저 읽어 합친 뒤 올린다.

2) 묶음 전체를 시각 하나로 비교했다.
   PC B 에서 할일 하나만 고쳐도 멘트·자료·고객메모까지 PC B 것으로 올라가
   PC A 의 멘트 수정이 그 순간 사라졌다. 앱을 켜 둔 채 지내면 거의 확실히
   이 길로 들어갔다(_maybe_autosync 가 수시로 push 한다).
   이제 파일마다 수정시각(mtime)으로 따로 판단한다. 받아 쓴 파일은 mtime 을
   보낸 쪽 시각으로 되돌린다 — 안 그러면 서로 밀어내는 핑퐁이 된다.

3) 목록은 항목 단위로 합친다.
   멘트 문구·자료검색 내 메모·계약조건·고객메모·팔로업기록·안내멘트기록 6개는
   양쪽을 합집합으로 둔다. 이름이 같으면 파일이 더 최신인 쪽을 쓴다.
   할일·서류체크는 합치지 않는다 — 완료·삭제가 정상 동작이라 합치면 지운 것이
   되살아난다.

4) 켜는 순간에만 받던 것을 10분 주기로 바꾸고, 설정에 [지금 동기화] 를 넣었다.
   앱을 띄워 둔 채로도 다른 PC 의 변경이 들어온다.

번들은 기존 모양(files: 문자열)을 유지하고 시각표(times)만 곁들여, 아직 옛
버전을 쓰는 PC 가 읽어도 지금과 똑같이 동작한다.

sync 는 그동안 검증이 없었다. 드라이브를 딕셔너리로, 계정 폴더를 둘로 나눠
두 PC 를 흉내 내는 하니스를 만들고 이번 사고 시나리오를 그대로 재현해 넣었다.

## v1.3.2 — 자료검색 화면 정리 · 링크 · 기본자료 덮어쓰기 (2026-08-29)

1) 읽기 편하게
   - 목록·부제에서 제목 뒤 [금융사 · 분류] 꼬리표를 뺐다. 캐피탈 자료는
     제목과 금융사가 같아 'SK렌터카 [SK렌터카 · 캐피탈]' 로 세 번 나왔다.
   - 캐피탈 본문 차례를 연락처 → 견적 홈페이지 → 전자약정 신청서 → 승계조건
     → 나머지 로 바꿨다. 상담 중에 여는 화면이라 연락처가 맨 위여야 한다.
   - 본문을 QTextBrowser 로 그려 주소를 눌러 바로 열 수 있게 하고, 덩어리
     사이에 점선을 넣었다. 저장은 지금처럼 평문이라 시트·복사는 그대로다.
     (Qt 는 <hr> 의 dashed 를 무시하고 실선으로 그려서 글자로 점선을 만들었다)
   - 목록과 내용 사이에 그어지던 검은 선 제거 — 스타일시트에 QSplitter 규칙이
     없어 시스템 기본 손잡이가 그려지고 있었다.

2) BNK캐피탈·메리츠캐피탈·신한카드에 전자약정 신청서 주소 추가.

3) 심사서류 기본 문구가 안 바뀌던 문제
   [기본 자료 불러오기] 가 제목이 겹치면 무조건 건너뛰어서, 앱에 새로 넣은
   문구가 이미 그 제목을 가진 PC 에는 영영 도달하지 못했다. 이제 내용이 다르면
   덮어쓸지 물어본다(예/아니오/취소). 묻지 않고 덮어쓰지는 않는다.

4) 그 과정에서 찾은 데이터 손상 두 건
   - 편집창의 QTextEdit 가 빈 줄을 '공백 한 칸이 든 줄' 로 돌려줘, 창을 열었다
     저장하기만 해도 고객에게 보내는 글이 조용히 바뀌었다. kb.norm_body() 로
     들고 나는 길목에서 줄 끝 공백을 턴다.
   - 본문 렌더링을 붙일 때 편집창까지 HTML 로 그려져, 저장하면 점선·머리글
     장식이 본문에 섞여 시트로 올라갈 뻔했다. 편집창은 평문으로 되돌리고
     회귀 검증에 이 경우를 넣었다.

## v1.3.1 — 자료검색 기본자료 채우기 — 심사서류 3 · 캐피탈 21 · 보험사 (2026-08-29)

v1.3.0 의 자료검색함에는 자리만 채운 예시 3건뿐이라 그대로는 쓸 수 없었다.
실제로 쓰시는 심사서류 안내문과, 손으로 관리하시던 승계조건표·연락처표를
기본자료로 옮긴다.

- 심사서류 3건(개인 / 개인사업자 / 법인) — 고객에게 그대로 보내는 안내문이라
  본문을 한 글자도 바꾸지 않았다. 번호 목록은 체크리스트로도 넣어,
  고객관리 [서류] 가 그대로 불러 쓴다.
- 캐피탈·렌터카 21곳 — 승계조건(납부횟수·잔여일·매매상사·수수료·정산일),
  전자약정 필요정보, 법인약정 서류, 운전자 범위, 견적 홈페이지, 연락처를
  한 장으로 합쳤다. 값이 없는 항목은 '-' 를 적지 않고 아예 뺀다.
- 보험사 사고접수 12곳은 한 건으로.

두 원본이 어긋나던 곳:
- 승계부서 번호가 다른 7곳은 연락처표 값을 썼다.
- DGB캐피탈 = iM캐피탈, M캐피탈 = MG캐피탈 로 합쳤다.
- 승계 잔여일이 다른 두 곳(SK렌터카·우리카드)만 그 줄에 다른 값을 병기했다.
- 우리카드 견적 사이트의 로그인 ID/PW 는 넣지 않았다 — 이 자료는 전 직원이
  보는 스프레드시트로 올라간다.

자료가 25건으로 늘어 로직과 섞이지 않게 kb_seed.py 로 분리했다.
이미 자료를 저장해 둔 PC 는 기본값이 보이지 않으므로, 편집창에
[기본 자료 불러오기] 를 두었다 — 제목이 겹치지 않는 것만 덧붙이고
고쳐 둔 자료는 건드리지 않는다.

## v1.3.0 — 자료검색함 · 서류 체크리스트 · 수당 미니계산기 · 백업 복원 (2026-08-29)

계약을 굴리는 과정이 비어 있던 부분을 메운다.

A) 자료검색 탭
   - 전자약정·심사서류·연락처를 미리 넣어 두고 '신한 전자약정' 이나
     'ㅅㅎ ㅈㅈㅇㅈ'(초성)으로 즉시 찾는다. v1.2.6 의 검색 엔진을 그대로 쓴다.
   - 회사 공통 자료는 수당율과 같은 전용 스프레드시트의 '업무자료' 탭에 두어
     어느 계정으로 로그인하든 같은 최신본을 본다(편집은 관리자 계정만).
     받아온 내용은 캐시해 두어 인터넷이 끊겨도 검색은 된다.
   - 개인 메모는 계정 폴더에 따로 두고 한 검색창에서 함께 찾는다.
   - 전역 단축키(기본 Ctrl+Alt+F)로 다른 프로그램을 쓰는 중에도 검색창이 뜬다.
     단축키를 두 개 쓰게 되어 hotkey 를 번호별 등록 방식으로 일반화했다
     (한 번호로 두 개를 등록하면 나중 것이 앞의 것을 밀어낸다).

B) 고객별 심사서류 체크리스트
   - 자료검색의 '심사서류' 목록을 고객에게 불러와 받은 것에 체크한다.
     받은 날짜가 함께 남고, 미제출 목록은 문장으로 복사된다.
   - 저장 키는 시트 U열 고객ID — 고객명을 바꿔도 체크가 안 끊긴다.
   - 진행 중인 계약에 미제출이 남으면 브리핑 [서류 미비] 에 뜬다.
     섹션이 하나 늘어 색을 재배정했다(만기 보라 / 서류 주황 / 이번주 회색).

F) 고객 등록·수정 창의 수당 미니계산기
   - 차량가를 넣으면 대리점 수당이 바로 계산된다. 차종은 '차종' 칸에 친 글자로
     추천하되 후보가 하나일 때만 고른다 — 가솔린/HEV 를 앱이 임의로 정하면
     금액이 조용히 틀린다. '하이브리드 → HEV' 같은 표기 차이는 맞춰 준다.
   - [적용] 을 눌러야 칸에 들어간다. 자동으로 넣으면 적어 둔 실제 수당을 덮어쓴다.

H) 백업 복원
   - 백업 대상이 할일·알람 3개뿐이라 설정·고객메모·멘트문구는 되돌릴 수 없었다.
     동기화 대상(sync._FILES) 전체로 넓혀 한 목록만 보게 했다.
   - 설정에 [백업 / 복원] 을 추가. 시각별로 묶은 시점을 골라 되돌리며,
     복원 직전 상태를 자동으로 백업해 되돌리기를 다시 되돌릴 수 있다.

## v1.2.7 — 만기 이력 조건·위치 수정 · 멘트복사 탭 카드 크기 (2026-08-20)

1) 고객 이력의 '만기'
   - 실제로 출고된 계약(진행현황 '출고' + 출고일 기재)에만 붙인다.
     발주만 하고 취소한 건까지 만기가 뜨던 것을 막는다.
   - 만기는 앞으로 올 날짜라 시간 역순 정렬에서 늘 맨 위를 차지했다.
     목록에서 빼 두었다가 마지막 줄에 붙여, 새 메모가 항상 위로 오게 했다.

2) 멘트복사 탭 (이전 '인삿말')
   - 화면 제목과 탭 이름을 '멘트 복사' 로 바꿨다.
   - 채널을 추가하면 그 줄의 카드만 커지던 문제를 고쳤다. 격자에 준
     여백(setRowStretch)이 다시 그릴 때 지워지지 않아, 예전에 여백이던 줄에
     카드가 들어오면 그 줄이 남은 공간을 전부 먹었다. 여백을 격자 바깥
     세로 상자로 옮기고 카드 세로 크기를 내용에 고정했다.

## v1.2.6 — 드롭다운 초성 검색 · 브리핑 강조 · 인삿말 탭 (2026-08-19)

1) 드롭다운을 검색형으로
   - 초성("ㅅㄴㅌ" → 쏘나타)과 낱말 일부("디엣지", "그랜저 하이")로 걸러진다.
   - 흰칸을 클릭하든 오른쪽 화살표를 누르든 목록이 바로 열린다.
   - ↑↓ 로 옮기고 Enter 로 선택. 한두 글자만 치고 Tab 을 눌러도 후보가
     하나면 그 값으로 채워진다.
   - 목록에 없는 값은 되돌린다. 다만 금융사·진행현황처럼 새 값을 적어야
     하는 칸은 그대로 둔다.
   - 편집칸이 생기면서 휠이 그리로 먼저 오므로, 휠 차단도 함께 넓혔다.

2) 브리핑을 한눈에 들어오게
   - 섹션 데이터를 먼저 만들고 평문/HTML 두 가지로 그린다(두 벌이 어긋나지
     않게). 화면은 이모지·색·크기를 입힌 HTML, [메모 복사]는 평문 그대로.
   - 높이는 줄 수 어림 대신 문서 렌더 높이를 직접 물어 스크롤을 줄였다.

3) 인삿말 탭 (고객관리 우측)
   - 쓰시던 HTML 도구를 그대로 옮겼다. 채널별 [상담후]/[부재중] 버튼을
     누르면 클립보드에 복사되고 버튼이 잠깐 초록으로 바뀐다.
   - 문구는 [문구 편집] 에서 고칠 수 있고 greetings.json 에 저장돼
     다른 PC 로도 동기화된다.

## v1.2.5 — 긴급: 견적서가 저장되지 않던 문제 (2026-08-19)

증상: 견적서를 붙여넣어 저장해도 시트 S열에 드라이브 주소가 남지 않고,
목록의 [보기] 버튼도 눌리지 않는 상태('—')로만 나왔다.

원인: v1.1.28 에서 '수식에서 주소를 뽑는' 함수를 추가하며 이름을 image_url 로
지었는데, 바로 위에 이미 같은 이름의 '파일 id → 드라이브 주소' 함수가 있었다.
나중 정의가 앞의 것을 덮어써서, 업로드 마지막 단계의 image_url(file_id) 가
주소를 만들지 못하고 "" 를 돌려줬다.
그 결과 시트에는 =IMAGE("", 1) 이 적혔고, 거기서 주소를 못 뽑으니 버튼도 꺼졌다.
업로드 자체는 성공했으므로 파일은 드라이브 '견적서' 폴더에 남아 있다.

수정:
- 새로 추가했던 함수를 doc_url 로 바꿔 이름 충돌을 없앴다(호출부 2곳도 변경).
- 같은 실수가 다시 나지 않도록 모듈 전체에서 최상위 함수·메서드 이름이
  중복 정의되는지 검사해 두었다(현재 중복 없음).
- 업로드 → 수식 기록 → 주소 추출 왕복을 검증에 추가했다.

## v1.2.4 — 캘린더 칸의 일정을 색칠한 칩으로 (2026-08-18)

달력 칸 안의 일정이 날짜 숫자와 같은 색·굵기로 그려져 눈에 안 들어왔다.
이제 둥근 띠(칩)를 칠하고 그 위에 굵은 글씨로 그린다.
한 칸에 일정이 여러 개면 색을 번갈아 써 서로 구분된다.
칩 색과 글자색은 테마 5종 각각에 맞춰 따로 정의했다.
칩 여백은 최소로 잡아, 한 칸에 들어가는 일정 수가 예전보다 줄지 않는다.

## v1.2.3 — 수당율 관리자 제한 · 휠 차단 확대 · 화면 테마 5종 (2026-08-18)

1) 수당율은 관리자 계정에서만 수정
   모두가 같은 값을 쓰는 회사 기준값이라 직원이 실수로 바꾸면 안 된다.
   지정 계정으로 로그인했을 때만 [차종별 수당율 관리] 가 열린다.
   다른 계정에서는 버튼이 잠기고, 이유를 함께 보여 준다.
   (읽기는 그대로 — 직원 PC 도 관리자가 저장한 값을 받아 계산에 쓴다)

2) 숫자·날짜·시각 칸도 휠로 안 바뀌게
   v1.1.28 에서 드롭다운만 막았는데, '알림 30분 전' 같은 숫자칸은 그대로였다.
   화면을 스크롤하다 값이 바뀌는 건 마찬가지라 QAbstractSpinBox 계열
   (숫자·날짜·시각)까지 막았다. 드롭다운은 목록을 펼친 상태에서만 휠이 듣는다.

3) 화면 테마를 5가지 중에서 고르도록
   다크/라이트 두 가지뿐이던 것을 이름 기반으로 바꾸고 팔레트를 늘렸다.
     기본(밝은 청회색) · 따뜻한 크림 · 바이올렛 · 다크 · 미드나잇(다크 보라)
   설정의 '다크 모드' 체크는 테마 선택으로 대체했다.
   구버전 설정 파일에는 theme 키가 없으므로 기존 darkMode 값으로 정한다.
   모든 팔레트가 같은 색 키를 갖도록 맞춰, 빠진 색이 검게 나오는 일이 없다.

## v1.2.2 — 수당율을 전용 스프레드시트 한 곳으로 고정 (2026-08-18)

v1.2.1 은 '각 PC 가 설정에 넣어 둔 고객관리 시트' 의 수당율 탭을 썼다.
기본값이 같아 결과적으로 한 곳에 모이긴 했지만, 누군가 설정에서 시트 주소를
바꾸면 그 순간 서로 다른 수당율로 계산하게 된다. 고정이 아니었다.

이제 수당율 전용 스프레드시트 한 개만 바라본다(config.RATES_SHEET_ID).
- 각 PC 가 어떤 고객관리 시트를 쓰든, 어느 계정으로 로그인하든 같은 값을 쓴다.
- 고객관리 연동을 꺼 둔 PC 도 계산기는 쓰므로, 시트 연동 여부와 무관하게
  구글에 연결돼 있으면 받아온다(기존엔 고객관리 조회에 얹혀 있었다).
- 권한이 없거나 조회에 실패하면 조용히 넘어가고 이 PC 에 저장된 마지막 값으로
  계산한다. 반대로 저장에 실패하면 '다른 PC 에는 반영되지 않는다'고 알리고
  편집 권한을 확인하라고 안내한다.

## v1.2.1 — 수당율 입력칸 제거 · 계정이 달라도 공유되도록 (2026-08-18)

1) 계산기에서 수당율·화물차 입력칸을 없앴다
   계산할 때 실수로 건드리면 잘못된 금액이 나온다. 이제 차종을 고르면
   '적용 수당율  4.3 %  (승용차 기준)' 처럼 읽기만 되고, 값 자체는
   [설정] → [차종별 수당율 관리] 에서만 바꾼다.

2) 다른 PC 에서 다른 계정으로 로그인해도 수당율이 같아지도록
   구글 드라이브 동기화는 계정별 개인 저장소라 계정이 다르면 전달되지 않는다.
   반면 고객관리 스프레드시트는 이미 여럿이 함께 보는 파일이므로,
   수당율을 그 안의 '수당율' 탭에 두어 모두가 같은 값을 쓰게 했다.
   - 설정에서 저장하면 이 PC 에 저장한 뒤 공유 탭에도 올린다(탭이 없으면 만든다).
     올리지 못하면 '다른 PC 에는 반영되지 않는다' 고 분명히 알린다.
   - 시트를 불러올 때 공유 탭을 함께 읽어, 값이 다르면 이 PC 에 반영하고
     계산기를 갱신한다.
   - 시트 연동이 꺼져 있으면 예전처럼 이 PC 에만 저장하고, 창에 그렇게 안내한다.

## v1.2.0 — 수당계산기 추가 · 남은 버그 3건 수정 (2026-08-18)

[수당계산기 탭]
고객관리 오른쪽에 탭을 추가했다. 현대 / 기아 두 브랜드만 두고 현대_율곡은 뺐다.
차종을 고르면 수당율과 화물차 여부가 자동으로 채워지고, 차량가·지급율·면세를
넣으면 지급수수료가 바로 나온다. 계산식은 받은 웹 계산기와 동일하다.
  면세면 차량가 ÷ 1.0533, 공급가는 화물 ÷1.1 / 승용 ÷1.1572,
  거기에 차종 수당율 × 지급율 × 0.967(3.3% 공제).
수당율은 제조사 정책에 따라 바뀌므로 [설정] → [차종별 수당율 관리…] 에서
직접 고칠 수 있게 별도 창을 만들었다(차종 추가·삭제, 기본값 복원 포함).
고친 값은 commission_rates.json 에 저장하고 다른 PC 와도 동기화한다.

[금액에 소수점·음수가 들어가면 잘못 계산되던 문제]
'₩1,234.56' 에서 점을 그냥 지워 123456(100배)이 됐고, '₩-1,000' 처럼 부호가
통화기호 뒤에 있으면 음수를 잃었다. 회계 표기 '(₩1,000)' 도 양수로 읽었다.
이제 소수점은 반올림하고 음수는 부호를 지킨다. 금액칸에 마이너스를 먼저
입력해도 지워지지 않는다.

[팔로업 스캔이 하루 한 번뿐이던 문제]
그날 늦게 만든 출고 일정은 새로고침해도 다음 날까지 등록되지 않았다.
중복은 일정별 기록(tracker.done)이 막아 주므로 매 조회마다 검사한다.
  ※ 이 과정에서 v1.1.24 회귀를 하나 발견해 함께 고쳤다 — followup.py 에
    config import 를 빠뜨려 팔로업 기록 저장이 조용히 실패하고 있었다.
    하루 1회 제한을 없앤 상태로 두면 새로고침마다 중복 등록될 뻔했다.

[고객명을 수정하면 안내멘트 '발송함' 표시가 풀리던 문제]
복사 이력을 순번+고객명으로 식별해, 이름을 조금만 고쳐도 표시가 사라지고
브리핑이 다시 알려 왔다. 이제 고객ID(시트 U열)를 키로 쓴다.
기존 기록은 ID 를 읽어올 때 자동으로 옮겨 붙여, 판올림 후에도 표시가 유지된다.

## v1.1.32 — 복사한 뒤 버튼 글자가 잘리던 문제 (2026-08-18)

안내멘트를 복사하면 버튼 글자가 '복사' → '✅ 복사됨' 으로 바뀌는데,
이 글자에 필요한 폭이 86px 이라 62px 짜리 칸을 넘어 오른쪽이 잘렸다.

- 보낸 표시를 '✓ 복사' 로 줄였다(70px). 초록색 표시는 그대로 두고,
  버튼에 '복사' 라는 동작 이름도 계속 남겨 다시 복사할 수 있음을 알 수 있게 했다.
- 안내멘트 칸을 62 → 76px 로 넓혀 여유를 뒀다.
  늘어난 폭은 고객명 열에서 자동으로 조정되므로 표 전체 폭은 그대로다.

## v1.1.31 — 견적서 오른쪽 흰 칸 제거 · 짧은 열 가운데 정렬 (2026-08-18)

1) 견적서 오른쪽에 흰 칸이 남던 문제
   표가 실제 크기를 갖는 건 고객관리 탭을 처음 열 때인데, 목록은 보통 그보다
   먼저 도착한다. 그 시점의 (아직 작은) 폭으로 계산해 놓고 다시 맞추지 않아
   탭을 열면 오른쪽이 허옇게 남았다.
   수정: 표 크기가 바뀔 때마다 열 폭을 다시 맞춘다. 남는 폭은 고객명이
   가져가고, 창이 표보다 좁으면 열을 찌그러뜨리지 않는다.
   폭을 바꾸면 스크롤바가 생겼다 사라지며 다시 불릴 수 있어 재진입도 막았다.

2) 순번·계약일·출고일·진행현황을 가운데 정렬
   짧은 값이라 왼쪽에 붙으면 읽기 나빴다.
   수수료는 금액이라 오른쪽 정렬을, 고객명·차종은 길어서 왼쪽 정렬을 유지한다.

## v1.1.30 — 자동 불러오기가 안 되던 문제 · 견적서 버튼 무반응 (2026-08-18)

1) 프로그램을 켜도 고객관리가 자동으로 안 불러와지던 문제
   구글 계정 확인이 끝나야 '그 계정의' 설정(시트 사용 여부·주소)을 알 수 있는데,
   그건 네트워크 왕복이라 몇 초 걸린다. 그런데 자동 불러오기는 시작 0.8초에
   딱 한 번만 시도하고, 아직 설정을 모르면 조용히 포기해 버렸다.
   그래서 아무리 기다려도 불러오기가 일어나지 않았다.
   (v1.1.25 에서 브리핑 때문에 2.5초 → 0.8초로 앞당기며 훨씬 자주 발생하게 됐다)

   수정: 준비될 때까지 1.5초 간격으로 다시 확인하고(최대 20초),
   계정 확인이 끝나 설정을 읽은 직후에도 곧바로 불러오기를 시작한다.
   중복 실행되지 않도록 한 번만 시작한다.

2) 견적서 [보기] 를 눌러도 반응이 없던 문제
   S열의 =IMAGE() 수식은 목록보다 늦게 도착하는데, 도착해도 표를 다시 그리지
   않았다. 그래서 버튼이 '이미지 없음(—)' 상태로 굳어 눌러도 아무 일이 없었다.
   수정: 수식이 도착해 값이 바뀌면 표를 다시 그린다.

## v1.1.29 — 창이 화면 밖으로 올라가 못 옮기던 문제 (2026-08-18)

증상: 프로그램을 켜면 창이 화면 왼쪽 위에 붙어 제목표시줄이 화면 밖으로
올라가 버려, 창을 끌어 옮길 수 없었다.

원인: 창 위치를 한 번도 지정하지 않아 배치를 창 관리자에 맡기고 있었다.
v1.1.28 에서 폭을 1120 으로 넓히면서 그 배치가 화면을 벗어나게 됐다.

수정:
- 시작할 때 창을 모니터 가운데로 옮긴다.
- 창틀 크기는 화면에 뜬 뒤에야 정확히 알 수 있으므로, 첫 표시 때 한 번 더
  맞춰 제목표시줄이 반드시 화면 안에 들어오게 한다.
- 화면보다 창이 클 때를 대비해 위쪽·왼쪽 가장자리를 넘지 않도록 잡아 둔다.
- 사용자가 옮긴 위치는 그대로 둔다(트레이로 내렸다 다시 열어도 자리 유지).

## v1.1.28 — 휠 잠금 · 견적서 보기 · 이력 더블클릭 · 계약조건 설정 · 수수료 열 (2026-08-18)

1) 드롭다운이 휠로 바뀌던 문제
   표를 스크롤하려다 그 아래 금융사·진행현황이 엉뚱하게 바뀌는 사고가 잦았다.
   앱 전체에 이벤트 필터를 걸어 드롭다운 위에서의 휠을 막는다.
   목록이 펼쳐진 상태에서는 원래대로 휠로 고를 수 있다.

2) 견적서/계약서 보기
   고객관리 표 맨 오른쪽에 [보기] 를 추가했다. S열의 =IMAGE() 주소를 읽어
   원본을 내려받아 팝업으로 크게 보여준다(창 폭에 맞춰 축소, 스크롤 지원).
   이미지가 없는 고객은 버튼이 비활성('—')이고, 내려받기에 실패하면
   [브라우저에서 열기] 로 안내한다.

3) 고객 목록 더블클릭 → 이력
   더블클릭이 수정창 대신 이력을 연다. 상단 [이력] 버튼은 없앴다.
   버튼이 놓인 안내멘트·견적서 열에서는 더블클릭이 동작하지 않는다.

4) 자주 쓴 계약조건을 직접 관리
   설정에 '자주 쓴 계약조건' 입력칸을 넣었다. 한 줄에 하나씩
   '금융사 | 계약조건' 형식이고, 금융사를 안 적으면 모든 금융사에 공통으로
   보인다. 직접 적은 것이 목록 위에 오고, 시트에서 실제로 써 온 조건도 뒤에
   함께 나온다. terms_presets.json 에 저장하고 다른 PC 와도 동기화한다.

5) 고객관리 표 정리
   - 안내멘트 오른쪽에 남던 흰 여백 제거: 남는 폭을 고객명 열이 흡수한다.
     창보다 표가 넓을 때는 열이 찌그러지지 않도록 '남을 때만' 더한다.
   - 수수료(합계) 열을 추가하고 금액을 오른쪽 정렬했다.
   - 열이 늘어 가로 스크롤이 생기지 않도록 창 폭을 1040 → 1120 으로 넓혔다.

## v1.1.27 — 긴급: U열 없는 시트에서 고객관리가 전혀 안 열리던 문제 (2026-08-17)

증상: 프로그램 실행 시 "시트 요청 실패 (HTTP 400) Range ('미출고차량'!U1:U)"
가 뜨고 고객 목록이 통째로 안 열림.

원인: v1.1.25 에서 고객ID(U열)를 목록 조회(batchGet)에 함께 넣었다.
시트가 T열까지만 있으면 U 는 격자 밖이라 400 이 나는데, batchGet 은 범위
하나만 잘못돼도 요청 전체가 실패한다. 그래서 멀쩡한 고객 목록까지 못 읽었다.
기본 시트는 보통 Z열까지 있어 개발 중에는 드러나지 않았다.

수정:
- 목록 조회에서 U 범위를 뺐다. 고객ID 는 안내멘트·견적서와 같이 목록이 뜬 뒤
  따로 읽고(read_uids), 실패하면 조용히 빈 값으로 둔다. 이력 기능만 잠시
  못 쓸 뿐 목록은 항상 열린다.
- ensure_uid_column 이 시트 격자를 먼저 확인해, U열이 없으면 열을 추가한 뒤
  머리글을 쓰고 숨긴다. 좁은 시트에서도 이력 기능을 쓸 수 있다.
- 고객 추가 시에도 U열을 미리 준비한다.

함께 고친 것:
- 고객ID 기록 실패를 조용히 삼키던 부분. ID 가 시트에 안 남으면 다음에 이력을
  열 때 새 ID 가 발급돼 방금 적은 메모를 못 찾게 되므로, 실패를 사용자에게 알린다.

## v1.1.26 — 취소 건 실적 제외 · 고객관리 열 폭 · 캘린더 수정/삭제 (2026-08-17)

1) 취소된 계약이 이번달 실적에 잡히던 문제
   진행현황이 '취소' 인 건은 출고일이 적혀 있어도 집계에서 뺀다.
   건수·수수료 합계는 물론 지난달 대비 증감과 금융사 분포까지 함께 바로잡힌다.

2) 고객관리 열 폭 조정
   안내멘트는 [복사] 버튼만 들어가므로 96 → 62 로 줄이고, 아낀 폭을
   고객명(+14) 금융사(+6) 차종(+8) 계약일(+3) 출고일(+3) 에 나눠 줬다.
   전체 폭은 그대로여서 가로 스크롤이 더 늘지 않는다.
   마지막 열이 남는 공간을 먹던 설정도 껐다(버튼 칸만 넓어지던 원인).

3) 캘린더에서 기존 일정 수정·삭제
   지금까지 추가만 되고 고칠 방법이 없었다.
   - [일정/할일] 의 일정을 더블클릭하면 캘린더 창이 그 날짜로 열리고
     해당 일정이 선택된다.
   - 캘린더 아래 목록에서 일정을 더블클릭하거나 [선택 일정 수정] /
     [선택 일정 삭제] 로 편집한다.
   - 수정창에서 제목·날짜·시각·종일 여부를 바꿀 수 있다. 다른 캘린더로
     옮기는 것은 구글에서 별도 move 요청이라 이번엔 막아 뒀다.
   - 이를 위해 CalEvent 에 캘린더 ID / 일정 ID 를 싣는다. iCalUID 는
     반복 일정이 공유하므로 조작 키로 쓸 수 없다.
   - 삭제는 화면에서 먼저 지우고, 결과가 오면 서버 기준으로 다시 맞춘다.

## v1.1.25 — 만기 재계약 알림 + 고객 이력 타임라인 (2026-08-17)

[만기 재계약 알림]
계약조건(M열)의 '60개월' 표기에서 계약기간을 읽어, 출고일(없으면 계약일)에
더해 만기일을 계산한다. 만기가 다가온 고객을 시작 브리핑에 알려준다.
- 말일 보정은 검증돼 있는 followup.add_months 를 재사용한다.
- 진행현황이 '취소' 인 고객, 계약조건에 개월 수가 없는 고객은 제외한다.
- 이미 지난 건도 30일까지는 남긴다 — 놓친 건이 조용히 사라지면 안 된다.
- 설정에 '만기 재계약 알림' 개월 수를 추가했다(기본 3, 0이면 끔).
  구버전 설정 파일에 키가 없어도 기본값으로 동작한다.

[고객 이력 타임라인]
고객관리에 [이력] 버튼을 추가했다. 계약·출고·안내멘트 발송·만기 예정일은
시트에서 자동으로 뽑아 보여주고, 상담 메모는 직접 남길 수 있다.
- 메모는 customer_notes.json 에 저장하고 다른 PC 와도 동기화한다.
- 저장은 v1.1.24 에서 만든 atomic_write 를 쓴다.

[고객 식별 — 시트 숨김 ID 열]
메모를 고객에 묶으려면 변하지 않는 키가 필요한데, 기존 '순번+고객명' 은 둘 다
변한다(이름 수정, 브라우저에서 행 삭제 시 순번 밀림). 그래서 U열에 앱이 만든
ID 를 적고 그 열을 숨긴다.
- 읽기는 기존 batchGet 에 범위만 추가해 요청 수가 늘지 않는다.
- 기존 고객은 [이력] 을 처음 열 때 그 고객에게만 발급한다(일괄 쓰기 안 함).
- 고객을 삭제하면 ID 도 비운다 — 그 행을 재사용한 새 고객에게 옛 이력이
  따라붙지 않게 한다.

[함께 고친 것]
- 안내멘트 복사 기록 저장이 v1.1.24 에서 원자적 쓰기로 못 바뀌어 있던 것을 수정.

## v1.1.24 — 데이터가 사라지는 버그 일괄 수정 (2026-08-17)

전체 코드를 감사해 '오류 없이 조용히 데이터를 파괴하는' 버그를 찾아 고쳤다.
대부분 "저장했습니다" 토스트까지 띄우면서 데이터를 지워, 사용자가 잃어버린
사실조차 알 수 없었다.

[시트]
- 고객 [수정] 저장이 견적서/계약서 이미지(S열)를 지우던 문제.
  =IMAGE() 수식은 표시값이 빈 문자열이라 그 빈 값을 되돌려 쓰고 있었다.
  이제 다이얼로그가 S열을 아예 내보내지 않고, 새 이미지를 넣거나 [지우기] 를
  누른 경우에만 기록한다. S열은 수식 그대로 따로 읽어 '이미지 있음' 을 표시한다.
- 고객 추가를 연달아 하면 같은 행에 겹쳐 써서 앞 고객이 사라지던 문제.
  행 번호를 UI 에서 미리 예약하고, 추가 자체도 한 번에 하나씩 처리한다.
- 앱이 못 읽는 날짜 형식('2026년 8월 14일' 등)이 저장 한 번에 지워지던 문제.
  인식 형식을 넓히고, 그래도 못 읽으면 사용자가 직접 고치지 않는 한 건드리지 않는다.
- 설정에서 시트/탭을 바꿔도 이전 행 번호를 그대로 써서 엉뚱한 줄을 덮어쓰던 문제.
- 삭제한 자리에 빈 줄이 영구히 쌓이던 문제.

[동기화·백업]
- 계정 확인에 실패하면 공용 폴더를 번들로 올려 다른 PC 데이터까지 지우던 문제.
  이제 계정을 모르면 동기화를 건너뛴다(이메일 조회도 몇 차례 재시도).
- 오프라인에서 한 작업이 다음 실행의 pull 에 덮어써지던 문제.
  로컬 변경 시각을 온·오프라인 무관하게 기록한다.
- 번들의 빈 항목이 다른 PC 의 멀쩡한 파일을 0바이트로 만들던 문제.
- 매일 백업이 계정 폴더가 아닌 공용 폴더를 백업하던 문제(안전망이 죽어 있었다).
- 저장이 원자적이지 않아 도중에 죽으면 목록이 통째로 날아가던 문제.
  임시파일 → 교체 방식으로 통일했다.

[할일·알람]
- 반복 할일을 [수정]하면 반복 설정이 사라지던 문제.
- 반복 할일을 [완료]하면 영구 삭제되던 문제(이제 다음 차례로 넘어간다).
- 늦게 도착한 조회 결과가 방금 추가한 할일을 지우던 문제(구글에 중복 생성으로 이어졌다).
- 구글 캘린더 정시 알람이 2~3번 울리던 문제.

[표시·업데이트]
- 조회 오류가 전혀 표시되지 않고, 토큰이 죽어도 '연결됨' 으로 보이던 문제.
- 캘린더 하나가 실패하면 그 일정만 조용히 사라지던 문제(부분 실패를 알린다).
- 잘린 다운로드를 검증 없이 설치하고 백업까지 지워 앱이 실행 불가가 되던 문제.
  크기를 검증하고 직전 버전을 남긴다.
- v1.1.23 회귀: 시트 상단 요약이 없으면 이번달 실적이 아예 안 보이던 문제.

## v1.1.23 — 빌드: 릴리스 빌드에서 아티팩트 업로드 생략 (저장 용량 한도로 릴리스가 막히던 문제) (2026-08-17)

v1.1.23 빌드는 exe 까지 정상 생성됐지만 아티팩트 업로드에서
'Artifact storage quota has been hit' 로 실패해, 뒤따르는 릴리스 단계가
실행되지 않았다. 매 빌드 52MB 짜리 exe 를 90일 보관으로 쌓아 온 탓이다.

- 릴리스를 만드는 빌드에서는 아티팩트를 올리지 않는다(릴리스에 exe 가 첨부되므로 중복).
- 그 외 빌드는 보관 기간을 5일로 줄이고, 실패해도 워크플로가 멈추지 않게 한다.

## v1.1.22 — 고객관리 삭제 버튼·상단 현황 색상·견적서 폴더 정리 (2026-08-16)

1. 고객관리에 [삭제] 추가. 직접 입력한 칸만 비우고 합계·안내멘트·등록완료 같은
   함수 칸은 그대로 둔다. 줄을 지우면 아래 순번이 밀리므로 내용만 비우며,
   순번(A)은 값일 때만 함께 비운다(수식이면 보존). 실패하면 되살린다.
2. 상단 발주/가망고객/지난달출고/이번달출고 숫자를 항목별 색상 + 굵게 표시.
   다크모드 전환 시에도 다시 계산되도록 했다.
3. 견적서 이미지는 드라이브 '견적서' 폴더에만 저장한다(폴더 생성 실패 시 업로드
   중단). PNG 시그니처를 확인해 사진이 아닌 데이터는 올리지 않는다.

## v1.1.21 — 종료 동작 선택·설정창 확대·시트 로딩 속도·순번 수정 (2026-08-16)

1. 창 닫기(X) 는 묻지 않고 바로 트레이로 내려간다(기본). 설정에 '창 닫기 버튼'
   항목을 추가해 트레이/완전 종료 중 고를 수 있게 했다.
2. 설정 화면을 메인 프로그램 높이만큼 키워 스크롤을 최소화했다.
3. 고객관리 불러오기 속도 개선
   - 목록과 상단 요약을 batchGet 요청 1회로 받는다(기존 2회).
   - 분량이 가장 큰 R열(고객안내멘트)을 첫 요청에서 빼고, 목록이 뜬 뒤
     뒤이어 채운다 → 첫 화면이 훨씬 빨리 뜬다.
   - 프로그램 시작 2.5초 뒤 백그라운드로 자동 불러오기(시작 지연 없음).
4. 새 고객 순번이 직전 번호와 같아지던 문제 수정. A열이 값이면 최대+1 을
   앱이 계산해 기록하고(108→109), 수식이면 건드리지 않는다.
   목록은 순번 내림차순으로 바꿔 최근 등록이 맨 위에 오게 했다.

## v1.1.20 — 고객관리 입력·목록 개선 (2026-08-16)

1. 새 행에 직전 행의 서식·드롭다운·조건부서식을 함께 복사(PASTE_FORMAT /
   PASTE_DATA_VALIDATION / PASTE_CONDITIONAL_FORMATTING). 값만 쓰면 새 행에
   데이터 확인 규칙이 없어 '대리점' 을 넣어도 시트에서 색이 안 입혀졌다.
2. 계약조건/내용 메모박스에서 Tab 으로 다음 입력창으로 이동(setTabChangesFocus).
3. 차량가격/금융수수료/대리점수당을 입력하는 즉시 1,000 단위로 구분해 표시하고,
   시트에는 콤마 없는 숫자로 기록해 통화 서식이 그대로 적용되게 했다.
4. 고객관리 표에 [안내멘트] 열 추가 — 행마다 복사 버튼. 한 번이라도 누른 고객은
   '복사됨' 으로 표시되고, 이력은 ment_copied.json 에 저장되어 다중 PC 동기화된다.
5. 진행현황 색상 구분(글자만, 굵게): 출고=파랑 / 발주=연초록 / 취소=빨강.
6. 연결이 안정화되어 [연결 진단] 버튼과 관련 코드를 제거했다.

## v1.1.19 — 고객관리 입력 개선 — 날짜 달력·이미지 붙여넣기·안내멘트 복사·발주 현황 (2026-08-15)

1. 계약일/출고일을 달력에서 고르고 시트 서식(2026. 8. 14)으로 저장.
   기존 값도 같은 서식으로 읽어오고, 아직 안 정해진 날짜는 [미정] 으로 비워 둔다.
2. 견적서/계약서를 Ctrl+V 로 붙여넣으면 드라이브에 올리고 셀에 =IMAGE() 로
   그림이 보이게 한다. 시트가 이미지를 가져오려면 '링크 공개'가 필요하므로
   최초 1회 확인을 받는다. 전용 폴더에 보관하고 권한은 보기 전용.
3. 등록완료(T열)는 시트 수식이므로 입력 항목에서 제거하고 보호 대상에 추가.
   하단 '자동 계산' 안내 대신 [고객안내멘트 복사] 버튼을 넣어 R열 내용을 복사.
4. 시트 N1:O4 의 발주 현황을 고객 목록 상단에 표시.

drive.file 권한이 추가되어 재로그인이 한 번 필요하다.

## v1.1.18 — 설정 화면 테마 복구 — 스크롤 영역 배경 회귀 수정 (2026-08-15)

v1.1.16 에서 설정 항목이 늘어 스크롤 영역을 추가했는데, theme.qss() 에
QScrollArea·뷰포트·내부 위젯에 대한 배경 규칙이 없었다. 게다가 앱이 QPalette 를
한 번도 설정하지 않아, 스타일시트가 닿지 않는 표면은 윈도우 시스템 다크 테마로
그려졌다 → 밝은 테마 글자 + 어두운 배경이 되어 그룹 제목이 묻혔다.

- theme: QScrollArea / 내부 위젯 / #dlgbody 배경 규칙 추가
- theme: palette() 추가 + apply_to_app() 으로 스타일시트·팔레트·색상스킴을 함께 적용
  → 스타일시트가 닿지 않는 표면도 시스템 테마가 아닌 앱 테마를 따른다(재발 방지)
- app/main_window: 시작 시와 다크모드 토글 시 모두 apply_to_app() 사용
- settings_dialog/customer_dialog: 내부 위젯에 objectName + WA_StyledBackground
  (캘린더 창의 #calwin 과 동일한 패턴), 스크롤 테두리 제거

검증: 다크 시스템 팔레트를 강제로 깔아도 라이트/다크 각 테마의 window_bg 로
렌더링되는 것을 픽셀 값으로 확인. 표/목록/캘린더 배경은 영향 없음.

## v1.1.17 — 시트 '권한 없음' 원인 진단 및 정확한 안내 (2026-08-15)

재로그인·동의를 마쳤는데도 계속 '권한 없음'이 나오는 문제.
원인은 sheets._check() 가 모든 401/403 을 '재로그인 하세요' 하나로 뭉쳐,
클라우드에 Sheets API 가 꺼져 있는 경우(재로그인으로는 절대 해결 안 됨)에도
같은 안내를 내보내 무한 재로그인을 유도한 것.

- sheets: 403 을 원인별로 분기(SERVICE_DISABLED / scope 부족 / 401 / 404 / 기타)
  하고 구글 응답 원문을 그대로 노출. API 미사용설정이면 사용설정 URL 을 안내.
- google_client: tokeninfo 로 실제 부여된 권한을 확인하는 granted_scopes() 추가.
  토큰 교환·갱신 시 부여된 scope 를 google_token.json 에 기록(구버전 파일 호환).
- 고객관리 탭에 [연결 진단] 버튼 — 권한 보유 여부와 시트 읽기를 각각 확인해
  '다음에 할 일' 을 한 줄로 지정해 준다(주소는 복사 가능).
- 로그인 직후 스프레드시트 권한이 안 붙었으면 그 자리에서 안내.
- 시트 기능 사용 전 저장된 scope 로 선제 확인(불필요한 403 왕복 제거).

## v1.1.16 — 할일/일정 추가 즉시 반영 + 휴대폰 알림 + 구글 시트 고객관리 연동 (2026-08-14)

- 즉시 반영(낙관적 UI): 추가·수정·삭제·완료 시 네트워크 전에 화면부터 갱신.
  UI 스레드에서 하던 블로킹 요청을 백그라운드로 옮기고, 실패하면 자동 롤백.
  epoch 기반 경합 방지로 방금 추가한 항목이 사라지거나 중복되지 않게 처리.
- 할일 조회가 75일치 캘린더 조회 뒤에 묶여 있던 직렬화 제거(각각 즉시 반영).
- 캘린더 창: POST 전에 일정 표시, 전체 재조회 2회 → 1회로 축소.
- 휴대폰 알림: 등록하는 일정에 구글 캘린더 리마인더 자동 설정(설정에서 분 조절).
- 구글 시트 고객관리 연동(신규 탭): 미출고차량 시트 읽기/추가/수정.
  함수 칸(순번·합계·고객센터번호·사고접수연락처·고객안내멘트)은 쓰지 않고
  직전 행 수식을 복사해 유지 — 직접기재 15개 칸만 칸 단위로 기록.
- 팔로업 토스트가 두 번 뜨고 리스트 원문이 노출되던 버그 수정.

## v1.1.15 — 자동 업데이트 안정화: PID 종료 대기+rename 트릭+한글경로(mbcs)+단일인스턴스 충돌 해결+진단로그 (2026-08-10)


## v1.1.14 — 캘린더 열기 즉시 표시(메인 일정 시드+지연 로딩) 속도 개선 + UI '멘트'→'메모' 전체 변경 (2026-08-10)


## v1.1.13 — 트레이/컨텍스트 메뉴 글자색 테마 적용(다크모드 가독성) + 중복 실행 방지(단일 인스턴스) (2026-08-09)


## v1.1.12 — 공휴일(대체공휴일 포함) 빨간색 표시 + 다크 더 어둡게/라이트 오프화이트 + 다크모드 캘린더 글자 가독성 수정 (2026-08-09)


## v1.1.11 — 캘린더 창 크기를 메인과 동일하게 (칸 확대, 잘림 방지) (2026-08-09)


## v1.1.10 — 캘린더 창 개선: 빈칸 제거 + 날짜칸에 일정명 직접 표시 (v1.1.10) (2026-08-09)

1. 달력이 창을 꽉 채우도록(stretch) + 불투명 배경 → 중간 빈칸/뒤 창 비침 제거
2. EventCalendar.paintCell 로 각 날짜 칸에 그 날 일정명 표시(클릭 불필요, 구글 캘린더 스타일)
   - 칸 넘치면 '+N건', 하단엔 선택일 상세 목록(고정 높이)

## v1.1.9 — 시작 표시 기본값 전체 + 내 할일 우클릭 컨텍스트 메뉴 (v1.1.9) (2026-08-09)

1. 프로그램 실행 시 표시 필터 기본값을 '이번주' → '전체'로 변경
2. 내 할일 목록 우클릭 시 추가/수정/삭제/완료/멘트 복사 메뉴 표시 (버튼과 동일 동작)

## v1.1.8 — 자동 업데이트(explorer 재실행) 시연 대상 (2026-08-09)


## v1.1.7 — 자동업데이트 교체 안정화 2차: 크기검증 + explorer.exe 재실행 (v1.1.7) (2026-08-09)

- 복사본이 원본 크기와 같아질 때까지 확인(부분 기록 방지)
- 재실행을 start 대신 explorer.exe 로 → 더블클릭과 동일 경로(가장 안정적)
- python312.dll 추출 실패(자기교체 조기실행) 대응

## v1.1.6 — 자동 업데이트 시연 대상 (수정된 교체 로직 검증용) (2026-08-09)


## v1.1.5 — 자동업데이트 교체 로직 안정화 (onefile 자기교체 손상 수정) + (2026-08-09)

- 새 exe를 대상 폴더에 스테이징 후, 파일 잠금 해제(앱 완전 종료)까지 copy 재시도
- 조기 실행/부분 기록으로 'No module PySide6.QtGui' 나던 문제 해결
- 교체 후 2초 대기 뒤 재실행

## v1.1.4 — 자동 업데이트 실동작 시연용 릴리스 (토큰 내장 확인 완료) (2026-08-09)


## v1.1.3 — 자동업데이트 토큰 내장본 배포 (직원 무설정) (2026-08-09)


## v1.1.2 — 자동업데이트 자산 매칭 수정 + (2026-08-09)

- GitHub이 한글 자산명을 default.exe로 바꾸는 문제 대응: 자산을 이름/라벨/첫 .exe 순으로 탐색
- 세션 토큰으로 비공개 릴리스 check()+download() 실제 검증 완료(50.9MB, 정상 PE)
- APP_VERSION 1.1.2 (수정된 업데이터를 담은 첫 배포본)

## v1.1.1 — 워크플로우에서 릴리스 배포(수동 태그 입력) 지원 (2026-08-09)

- workflow_dispatch 에 release_tag 입력 추가 → 서버에서 해당 태그로 Release 생성 + exe 첨부
  (로컬 태그 push 가 프록시로 막혀 서버측에서 릴리스 생성)
- APP_VERSION 1.1.1 (자동 업데이트 테스트용: 기존 1.1.0 사용자가 감지)

